# 신규 덤프 - [링크](https://www.krdump.com/Amazon.SAA-C03-KR.Premium.Version.html#exams)

---
## Q1. 한 회사가 여러 가용성 영역에 배포된 Amazon RDS 인스턴스에서 실행되는 데이터베이스를 호스팅합니다. 주기적 스크립트는 데이터베이스를 쿼리하여 중요한 애플리케이션에 부정적인 영향을 미칩니다. 최소한의 비용으로 애플리케이션 성능을 개선할 수 있는 방법은 무엇입니까?
> 정답 : D 

- A. 스크립트에 활성 연결이 가장 적은 인스턴스를 식별하고 해당 인스턴스를 쿼리하는 기능을 추가합니다.

- B. 개발팀에 하루가 끝나면 수동으로 새 항목을 내보내도록 지시합니다.

- C. Amazon ElastiCache를 사용하여 스크립트가 실행하는 일반적인 쿼리를 캐시합니다.

- D. 데이터베이스의 읽기 복제본을 만듭니다. 읽기 복제본만 쿼리하도록 스크립트를 구성합니다.

---

# Q2. 한 회사가 Auto Scaling 그룹으로 구성된 여러 Amazon EC2 인스턴스에 웹사이트를 호스팅하고 있습니다. 사용자들은 매주 주말 오후 6시부터 11시 사이의 피크 시간대에 응답 속도가 느리다고 보고하고 있습니다. 솔루션 아키텍트는 이러한 피크 시간대의 성능을 개선하기 위한 솔루션을 구현해야 합니다.
이러한 요구 사항을 충족하는 가장 운영 효율적인 솔루션은 무엇입니까?
> 정답 : D

- A. 메모리 사용률이 70%를 초과할 때 인스턴스를 추가하도록 대상 추적 확장 정책을 생성합니다.

- B. 자동 스케일링 그룹의 쿨다운 기간을 구성하여 피크 시간 전후의 원하는 용량을 조정할 수 있습니다.

- C. 피크 시간 전에 원하는 용량을 늘리기 위해 AWS Lambda 함수를 호출하는 예약된 Amazon EventBridge 규칙을 생성합니다.

- D. 피크 시간 전후에 원하는 용량을 변경하기 위해 반복 옵션이 있는 예약된 확장 작업을 구성합니다.


---
# Q3. 한 회사가 온프레미스 데이터 센터 네트워크를 새로운 VPC에 연결해야 합니다. 데이터 센터 네트워크는 100Mbps 대칭형 인터넷 연결을 사용합니다. 온프레미스에서 실행되는 애플리케이션은 매일 수 기가바이트의 데이터를 전송합니다. 이 애플리케이션은 Amazon Data Firehose 전송 스트림을 사용하여 데이터를 처리합니다.
최대 성능을 위해 솔루션 아키텍트는 무엇을 권장해야 합니까?
> 정답 : D

- A. AWS Snowball Edge Storage Optimized 기기를 구매하세요. 며칠 분량의 데이터가 누적되면 해당 기기에 데이터를 복사하고 AWS로 보내 Firehose로 신속하게 전송하세요.
필요에 따라 반복하세요.

- B. 온프레미스 네트워크와 VPC 간에 VPC 피어링 연결을 생성합니다. 온프레미스 네트워크가 VPC 피어링 연결을 사용하도록 라우팅을 구성합니다.

- C. 온프레미스 네트워크와 VPC 간에 AWS Site-to-Site VPN 연결을 생성합니다. 고객 게이트웨이와 가상 프라이빗 게이트웨이 간에 BGP 라우팅을 구성합니다. VPN 연결을 사용하여 온프레미스에서 Firehose로 데이터를 전송합니다.

- D. AWS PrivateLink를 사용하여 VPC에 Firehose용 인터페이스 VPC 엔드포인트를 생성합니다. 온프레미스 네트워크와 AWS 간에 1Gbps AWS Direct Connect 연결을 설정합니다. PrivateLink 엔드포인트를 사용하여 온프레미스에서 Firehose로 데이터를 전송합니다.

---

# Q4. 한 회사에는 Amazon DynamoDB 테이블에 사용자 거래를 저장하는 웹 애플리케이션이 있습니다. 규정을 준수하기 위해 회사는 사용자 거래 데이터 사본을 7년 동안 보관해야 합니다.
어떤 솔루션이 운영 비용을 최소화하면서 이러한 요구 사항을 충족할 수 있을까요?
> 정답 : C

- A. DynamoDB를 사용하여 테이블의 온디맨드 백업을 생성합니다. 백업을 Amazon S3 버킷에 저장합니다. S3 버킷에 대한 S3 수명 주기 구성을 설정합니다.
- B. AWS Lambda 함수를 호출하는 Amazon EventBridge 규칙을 생성합니다. Lambda 함수가 테이블을 백업하고 Amazon S3 버킷에 백업을 저장하도록 구성합니다. S3 버킷에 대한 S3 수명 주기 구성을 설정합니다.
- C. AWS Backup을 사용하여 테이블에 대한 백업 일정과 보존 정책을 만듭니다.
- D. DynamoDB 시점 복구를 사용하여 테이블을 지속적으로 백업합니다.

---

# Q5. 로펌에서는 특정 미래 날짜까지 수정이나 삭제를 방지하면서 파일을 공개적으로 읽을 수 있게 하려면 어떻게 해야 할까요?
> 정답 : C

- A. 정적 웹사이트 호스팅을 위해 S3 버킷에 파일을 업로드합니다. 보관 기간이 있는 S3 객체 잠금을 사용합니다. 읽기 전용 IAM 권한을 부여합니다.

- B. S3 버킷을 만듭니다. S3 버전 관리를 활성화합니다. AWS Lambda로 이벤트 트리거를 구성하여 개인 S3 버킷에서 수정된 객체를 복원합니다.

- C. S3 버킷을 만듭니다. S3 버전 관리를 활성화합니다. 보관 기간이 있는 S3 객체 잠금을 사용합니다. CloudFront 배포를 만듭니다. 버킷 정책을 사용하여 액세스를 제한합니다.

- D. 정적 웹사이트 호스팅을 위해 구성된 Amazon S3 버킷에 파일을 업로드합니다. 모든 AWS 주체에게 읽기 전용 IAM 권한을 부여합니다.

---
# Q6. 한 회사가 여러 개의 프라이빗 서브넷으로 구성된 VPC를 보유하고 있으며, 각 서브넷에는 여러 애플리케이션이 호스팅됩니다. 이러한 애플리케이션은 인터넷에서 접근할 수 없어야 합니다. 하지만 여러 AWS 서비스에 접근해야 합니다. 또한, 애플리케이션은 AWS 서비스에 접근하기 위해 공용 IP 주소를 사용해서는 안 됩니다.
> 정답 : C

- A. 각 프라이빗 서브넷에 인터넷 게이트웨이를 배포합니다. 프라이빗 서브넷에서 발생하는 트래픽을 인터넷 게이트웨이를 통해 라우팅합니다.

- B. 프라이빗 서브넷 간에 AWS Direct Connect 연결을 설정합니다. 프라이빗 서브넷에서 Direct Connect 연결을 통해 트래픽을 라우팅합니다.

- C. 필요한 AWS 서비스에 대한 인터페이스 VPC 엔드포인트를 구성합니다. 프라이빗 서브넷에서 인터페이스 VPC 엔드포인트를 통해 트래픽을 라우팅합니다.

- D. 각 프라이빗 서브넷에 NAT 게이트웨이를 배포합니다. 프라이빗 서브넷에서 발생하는 트래픽을 NAT 게이트웨이를 통해 라우팅합니다.

---

# Q7. 한 회사가 Amazon EC2 인스턴스를 사용하여 퍼블릭 웹 애플리케이션에 대한 요청을 처리합니다. 해당 애플리케이션은 URL 경로를 사용하여 트래픽을 여러 애플리케이션 페이지로 라우팅합니다.
회사는 예측할 수 없는 시간에 엄청난 트래픽 급증을 경험하기 시작했습니다. 트래픽 급증으로 인해 웹 애플리케이션에 문제가 발생하고 가끔씩 서비스를 이용할 수 없게 되었습니다.
회사는 트래픽이 갑자기 늘어나는 것을 처리하기 위해 웹 애플리케이션의 확장성을 높여야 합니다.
어떤 솔루션이 이 요구 사항을 충족시킬까요?
> 정답 : D

- A. 애플리케이션 로드 밸런서(ALB)를 생성합니다. 웹 애플리케이션 인스턴스를 대상으로 설정합니다. ALB 트래픽 지표를 기반으로 Amazon CloudWatch 알람을 생성합니다. 트래픽 급증 시 활성화되도록 알림을 구성합니다.

- B. 웹 애플리케이션 인스턴스의 Amazon Machine Image(AMI)를 생성합니다. AMI를 사용하여 별도의 가용 영역에 두 개의 웹 애플리케이션 인스턴스를 더 생성합니다. 세 인스턴스를 모두 참조하도록 웹사이트 DNS 레코드를 업데이트합니다.

- C. 애플리케이션의 Docker 이미지를 생성합니다. Amazon Elastic Container Service(Amazon ECS)를 사용하여 Auto Scaling ECS ​​클러스터를 생성합니다. 관리형 확장을 활성화합니다. 네트워크 로드 밸런서를 생성합니다. ECS 클러스터를 대상 그룹으로 설정합니다.

- D. 웹 애플리케이션 인스턴스의 Amazon Machine Image(AMI)를 생성합니다. AMI를 사용하여 최소 용량이 2개인 EC2 인스턴스로 구성된 Auto Scaling 그룹을 생성합니다. 애플리케이션 로드 밸런서를 생성합니다. Auto Scaling 그룹을 대상 그룹으로 설정합니다.

---

# Q8. 한 게임 회사에 게임 점수를 표시하는 웹 애플리케이션이 있습니다. 이 애플리케이션은 애플리케이션 로드 밸런서(ALB) 뒤의 Amazon EC2 인스턴스에서 실행됩니다. 이 애플리케이션은 MySQL 데이터베이스용 Amazon RDS에 데이터를 저장합니다.
사용자들은 데이터베이스 읽기 성능 저하로 인해 장시간 지연과 중단을 겪고 있습니다. 회사는 사용자 경험을 개선하고자 합니다.
어떤 솔루션이 이 요구 사항을 충족할 수 있을까요?
> 정답 : B

- A. EC2 인스턴스에서 AWS Lambda 함수로 애플리케이션을 마이그레이션합니다.

- B. 데이터베이스 앞에 Amazon ElastiCache(Redis OSS) 캐시를 사용합니다.

- C. Amazon Aurora 글로벌 데이터베이스를 사용하여 여러 AWS 지역에 걸쳐 여러 개의 읽기 복제본을 만듭니다.

- D. 애플리케이션과 데이터베이스 사이에 Amazon RDS Proxy를 사용합니다.


---

# Q9. 한 회사가 기본 AWS 계정의 VPC에서 실행되는 AWS Lambda 함수를 생성해야 합니다. Lambda 함수는 회사가 Amazon Elastic File System(Amazon EFS) 파일 시스템에 저장한 파일에 액세스해야 합니다. EFS 파일 시스템은 보조 AWS 계정에 있습니다. 회사가 파일 시스템에 파일을 추가함에 따라 솔루션은 수요에 맞춰 확장되어야 합니다.
어떤 솔루션이 이러한 요구 사항을 가장 비용 효율적으로 충족할 수 있을까요?
> 정답 : B

- A. 파일 시스템의 내용을 Lambda 계층으로 이동합니다. 회사의 보조 계정에서 Lambda 계층을 사용할 수 있도록 Lambda 계층의 권한을 구성합니다.

- B. 기본 계정과 보조 계정에 있는 VPC 간에 VPC 피어링 연결을 만듭니다.

- C. 파일 시스템에 대해 구성된 마운트를 갖는 보조 계정에 두 번째 Lambda 함수를 생성합니다. 기본 계정의 Lambda 함수를 사용하여 보조 계정의 Lambda 함수를 호출합니다.

- D. 기본 계정에 새 EFS 파일 시스템을 만듭니다. AWS DataSync를 사용하여 원본 EFS 파일 시스템의 내용을 새 EFS 파일 시스템으로 복사합니다.

---

# Q10. 솔루션 아키텍트는 Amazon CloudFront와 Amazon S3 오리진을 사용하여 정적 웹사이트를 제공하는 솔루션을 설계해야 합니다. 또한, 모든 웹사이트 트래픽을 검사하기 위해 AWS WAF를 사용해야 합니다.
> 정답 : B

- A. CloudFront IP 주소만 Amazon S3에 액세스할 수 있도록 보안 그룹을 구성합니다. AWS WAF를 CloudFront 배포에 연결합니다.

- B. 원본 S3 버킷을 보호하기 위해 원본 액세스 제어(OAC)를 사용하도록 CloudFront 및 Amazon S3를 구성합니다. AWS WAF를 CloudFront 배포에 연결합니다.

- C. CloudFront가 S3 오리진에서 콘텐츠를 요청하기 전에 모든 수신 요청을 AWS WAF로 전달하도록 CloudFront를 구성합니다.

- D. AWS WAF Amazon 리소스 이름(ARN)에서 오는 요청만 허용하도록 S3 버킷 정책을 구성합니다.

---

# Q12. 한 회사가 VPC에서 비디오 스트리밍 웹 애플리케이션을 호스팅합니다. 이 회사는 네트워크 로드 밸런서(NLB)를 사용하여 실시간 데이터 처리를 위한 TCP 트래픽을 처리합니다. 애플리케이션에 대한 무단 액세스 시도가 있었습니다.
회사에서는 최소한의 아키텍처 변경으로 애플리케이션 보안을 개선해 승인되지 않은 애플리케이션 액세스 시도를 방지하고자 합니다.
어떤 솔루션이 이러한 요구 사항을 충족시킬까요?
> 정답 : C

- A. AWS Shield Advanced를 사용하면 강화된 DDoS 보호 기능을 제공하고 무단 액세스 시도를 방지할 수 있습니다.

- B. 승인되지 않은 트래픽을 필터링하기 위해 NLB에 직접 일련의 AWS WAF 규칙을 구현합니다.

- C. 신뢰할 수 있는 IP 주소만 허용하는 보안 그룹으로 NLB를 다시 만듭니다.

- D. 엄격한 IP 주소 허용 목록으로 구성된 기존 NLB와 병렬로 두 번째 NLB를 배포합니다.

---

# Q13. 솔루션 아키텍트가 Amazon Aurora MySQL DB 인스턴스에 쿼리를 실행하는 AWS Lambda 함수를 생성했습니다. 솔루션 아키텍트가 테스트를 수행했을 때 DB 인스턴스에 연결 수가 너무 많다는 오류가 표시됩니다.
어떤 솔루션이 최소한의 운영 노력으로 이러한 요구 사항을 충족할 수 있을까요?
> 정답 : B

- A. DB 인스턴스에 대한 읽기 복제본을 생성합니다. 기본 DB 인스턴스 대신 복제본 DB 인스턴스를 쿼리합니다.

- B. Amazon RDS Proxy에서 프록시를 생성합니다. DB 인스턴스 대신 프록시를 쿼리합니다.

- C. 다중 AZ 배포를 위해 Amazon Aurora MySQL DB 인스턴스를 구성합니다.

- D. 데이터를 Amazon DynamoDB 데이터베이스로 마이그레이션합니다.

---

# Q14. 한 회사가 AWS Fargate 온디맨드 용량을 사용하는 Amazon ECS 클러스터에서 애플리케이션을 운영하고 있습니다. 이 애플리케이션은 갑작스러운 중단을 허용할 수 없습니다. 회사는 애플리케이션 비용을 최적화하고 애플리케이션이 항상 정상적으로 작동하도록 보장하고자 합니다. 어떤 솔루션이 이러한 요구 사항을 충족할까요?
> 정답 : C

- A. 롤링 업데이트 배포 유형에서 온디맨드 용량 대신 Fargate 스팟 용량을 사용합니다.

- B. 전환 가능한 예약 인스턴스를 구매하세요.

- C. 컴퓨팅 비용 절감 플랜을 구매하세요.

- D. 온디맨드 용량 예약을 생성합니다.

---

# Q15. 한 회사가 Amazon API Gateway REST API와 AWS Lambda 함수를 사용하여 API를 개발했습니다. 이 API는 전 세계 사용자에게 정적 콘텐츠와 동적 콘텐츠를 제공합니다. 이 회사는 API 요청에 대한 콘텐츠 전송 지연 시간을 줄이고자 합니다. 어떤 솔루션이 이러한 요구 사항을 충족할까요?
> 정답 : C

- A. REST API를 지역 API 엔드포인트로 배포합니다. 캐싱을 활성화합니다. Lambda 함수에 대한 예약된 동시성을 구성합니다.

- B. REST API를 지역 API 엔드포인트로 배포합니다. 캐싱을 활성화합니다. API 정의에서 콘텐츠 인코딩을 활성화하여 전송 중인 애플리케이션 데이터를 압축합니다.

- C. REST API를 엣지 최적화 API 엔드포인트로 배포합니다. 캐싱을 활성화합니다. API 정의에서 콘텐츠 인코딩을 활성화하여 전송 중인 애플리케이션 데이터를 압축합니다.

- D. REST API를 엣지 최적화 API 엔드포인트로 배포합니다. 캐싱을 활성화합니다. Lambda 함수에 대한 예약된 동시성을 구성합니다.

---

# Q17. 한 회사가 AWS에서 게임 애플리케이션을 운영하고 있습니다. 이 회사는 자사 게임 서버에서 원격 측정 데이터를 수집, 시각화 및 분석해야 합니다. 회사는 게임 서버의 동작, 성능 및 상태에 대한 인사이트를 거의 실시간으로 확보하고자 합니다. 어떤 솔루션이 이러한 요구 사항을 충족시킬까요?
> 정답 : C

- A. Amazon Data Firehose를 사용하여 거의 실시간으로 원격 측정 데이터를 수집, 처리 및 저장합니다. AWS Glue를 사용하여 Firehose에서 분석에 필요한 형식으로 데이터를 추출, 변환 및 로드(ETL)합니다. Amazon QuickSight를 사용하여 데이터를 시각화하고 분석합니다.

- B. Amazon Kinesis Data Streams를 사용하여 원격 측정 데이터를 수집, 처리 및 저장합니다. Amazon EMR을 사용하여 분석에 필요한 형식으로 거의 실시간으로 데이터를 처리합니다. Amazon Athena를 사용하여 데이터를 분석하고 시각화합니다.

- C. Amazon Kinesis Data Streams를 사용하여 원격 측정 데이터를 수집합니다. Amazon Managed Service for Apache Flink를 사용하여 거의 실시간으로 데이터를 처리하고 Amazon CloudWatch에 사용자 지정 지표를 게시합니다. Amazon CloudWatch를 사용하여 사용자 지정 지표를 기반으로 대시보드와 경보를 생성합니다.

- D. Amazon DynamoDB Streams를 사용하여 원격 측정 데이터를 수집하고 저장합니다. DynamoDB Streams를 구성하여 AWS Lambda 함수를 호출하여 거의 실시간으로 데이터를 처리합니다. Amazon Managed Grafana를 사용하여 데이터를 시각화하고 분석합니다.

---

# Q18. 한 회사가 여러 팀에 속한 리소스를 포함하는 단일 AWS 계정을 보유하고 있습니다. 이 회사는 각 팀과 관련된 비용을 파악해야 합니다. 이를 위해 CostCenter라는 태그를 사용하여 각 팀에 속한 리소스를 식별하고자 합니다. (2개를 고르시오)
> 정답: B,C

- A. 기존 AWS 계정에서 통합 청구를 설정합니다.

- B. 각 팀에 속한 모든 리소스에 사용자 정의 CostCenter 태그를 지정합니다.

- C. 비용 배분을 추적하려면 CostCenter 태그를 활성화하세요.

- D. 각 팀에 태그를 생성하고 값을 CostCenter로 설정합니다.

- E. AWS 청구 및 비용 관리를 구성하여 이메일 메시지를 통해 회사에 매월 청구서를 보냅니다.

---

# Q19. 한 회사가 Amazon EC2 인스턴스에서 실행되는 Amazon EMR 클러스터에서 임시 워크로드를 실행하려고 합니다.
해당 회사는 코어 노드에는 온디맨드 인스턴스를, 태스크 노드에는 스팟 인스턴스를 사용하려고 합니다. 또한, 회사가 운영하는 AWS 리전에서 EMR 클러스터를 배포하기 위해 메모리 최적화 EC2 인스턴스를 사용하려고 합니다.
해당 회사는 여러 가용 영역에 여러 서브넷을 구성했습니다. 회사는 지정된 인스턴스 유형 및 구매 옵션을 사용할 수 있는 가용 영역에서만 EMR 클러스터가 시작되도록 해야 합니다.
어떤 솔루션이 가장 높은 운영 효율성을 보이면서 이러한 요구 사항을 충족할까요?
> 정답 : D

- A. 인스턴스 그룹 구성으로 Amazon EMR을 배포합니다. 단일 서브넷에 EMR 클러스터를 시작합니다. EMR 클러스터 시작 시 가용 영역에 필요한 용량이 있는지 확인하도록 Amazon EMR을 구성합니다.

- B. 인스턴스 플릿 구성으로 Amazon EMR을 배포합니다. 단일 서브넷에 EMR 클러스터를 시작합니다. EMR 클러스터 시작 시 가용 영역에 필요한 용량이 있는지 확인하도록 Amazon EMR을 구성합니다.

- C. 인스턴스 그룹 구성으로 Amazon EMR을 배포합니다. 여러 가용 영역과 연결된 여러 서브넷에 EMR 클러스터를 시작합니다.

- D. 인스턴스 플릿 구성으로 Amazon EMR을 배포합니다. 여러 가용 영역과 연결된 여러 서브넷에 EMR 클러스터를 시작합니다.


--- 

# Q20. 한 회사가 온프레미스 비디오 처리 애플리케이션을 AWS로 마이그레이션하려고 합니다. 처리 시간은 5분에서 30분 사이입니다. 애플리케이션은 여러 작업을 병렬로 실행해야 합니다. 이 애플리케이션은 사용자가 Amazon S3 버킷에 업로드한 비디오를 처리합니다.
어떤 솔루션이 운영 부담을 최소화하면서 이러한 요구 사항을 충족할까요?
> 정답 : D

- A. S3 버킷을 구성하여 S3 이벤트 알림을 Amazon SQS FIFO 큐로 전송합니다. Amazon EC2 인스턴스에 애플리케이션을 배포합니다. SQS 큐 크기에 따라 확장되도록 Auto Scaling 그룹을 생성합니다.

- B. S3 버킷을 구성하여 S3 이벤트 알림을 Amazon SNS 토픽으로 전송합니다. 애플리케이션을 AWS Lambda 함수로 배포합니다. SNS 토픽을 구성하여 Lambda 함수를 호출합니다.

- C. S3 버킷이 S3 이벤트 알림을 Amazon SQS 표준 큐로 전송하도록 구성합니다. 애플리케이션을 AWS Lambda 함수로 배포합니다. Lambda 함수가 SQS 큐를 폴링하도록 구성합니다.

- D. S3 버킷이 S3 이벤트 알림을 Amazon SQS 표준 큐로 전송하도록 구성합니다. Amazon ECS 클러스터에 애플리케이션을 배포합니다. SQS 큐 크기에 따라 AWS Fargate 작업에 대한 자동 확장을 구성합니다.

---

# Q21. 한 회사가 Amazon RDS for MySQL을 사용하여 핵심 애플리케이션을 배포하고 있습니다. 이 애플리케이션은 높은 가용성을 보장해야 하며 자동 복구 기능도 갖춰야 합니다. 또한, 대화형 사용자(트랜잭션 쿼리)와 배치 보고(분석 쿼리) 모두 최대 4시간의 지연 시간 내에 처리해야 합니다. 분석 쿼리는 트랜잭션 쿼리의 성능에 영향을 미치지 않아야 합니다.
어떤 솔루션이 이러한 요구 사항을 충족할까요?
> 정답 : B

- A. 하나의 대기 인스턴스를 사용하는 다중 AZ DB 인스턴스 배포에서 Amazon RDS for MySQL을 구성합니다.
트랜잭션 쿼리는 기본 DB 인스턴스를 가리키도록 하고, 분석 쿼리는 다른 가용 영역에서 실행되는 보조 DB 인스턴스를 가리키도록 하십시오.

- B. 두 개의 대기 인스턴스가 있는 다중 AZ DB 클러스터 배포에서 Amazon RDS for MySQL을 구성합니다.
트랜잭션 쿼리는 기본 DB 인스턴스를 가리키도록 하고, 분석 쿼리는 판독기 엔드포인트를 가리키도록 하십시오.

- C. 여러 가용 영역에 걸쳐 여러 읽기 복제본을 사용하도록 Amazon RDS for MySQL을 구성합니다.
트랜잭션 쿼리는 기본 DB 인스턴스를 가리키도록 하고, 분석 쿼리는 다른 가용 영역에 있는 복제본 중 하나를 가리키도록 하십시오.

- D. 자동 백업이 활성화된 상태로 Amazon RDS for MySQL을 트랜잭션 쿼리용 기본 데이터베이스로 구성합니다. 매일 밤, 분석 쿼리를 지원하기 위해 가장 최근 스냅샷에서 읽기 전용 데이터베이스를 생성합니다. 이전에 생성된 데이터베이스는 종료합니다.

---

# Q.23 한 회사가 모바일 게임 앱을 운영하고 있는데, 이 앱은 세션 데이터(최대 256KB)를 최대 48시간 동안 저장합니다. 데이터는 자주 업데이트되며, 만료 후에는 자동으로 삭제되어야 합니다. 또한, 데이터 복구 기능도 필요합니다.
> 정답 : D

- A. 세션 데이터를 S3 Standard에 저장합니다. 버전 관리를 활성화하고 48시간 후에 객체가 만료되도록 수명 주기 규칙을 설정합니다.

- B. Amazon MemoryDB를 사용하고 PITR 및 TTL을 활성화하십시오.

- C. 버전 관리 및 48시간 후 만료되는 수명 주기 규칙을 사용하여 S3 지능형 계층화에 데이터를 저장합니다.

- D. 세션 데이터를 저장하는 데 Amazon DynamoDB 테이블을 사용합니다. 시점 복구(PITR) 및 TTL을 활성화합니다.

---

# Q24. 기업은 규제 요건을 충족하기 위해 아마존 S3에 저장된 중요 문서를 정해진 보존 기간 동안 삭제 또는 수정으로부터 보호해야 합니다. 어떤 솔루션이 이러한 요구 사항을 충족할까요?
> 정답 : C

- A. 거버넌스 모드에서 S3 객체 잠금을 활성화합니다.

- B. 수명 주기 삭제 규칙을 사용하여 S3 버전 관리를 활성화합니다.

- C. 규정 준수 모드에서 S3 객체 잠금을 활성화합니다.

- D. S3 Glacier Flexible Retrieval로 전환 객체.

---

# Q25. 한 소매 회사가 AWS에서 마이크로서비스 아키텍처를 사용하여 주문 처리 시스템을 구축하고 있습니다. 이 시스템은 접수된 주문을 처리 완료 시까지 안전하게 보관해야 합니다. 여러 팀의 서비스가 정의된 워크플로에 따라 주문을 처리합니다. 서비스는 확장 가능하고, 결합도가 낮으며, 주문량의 급증에도 대응할 수 있어야 합니다. 각 주문의 처리 단계는 중앙에서 추적되어야 합니다.
어떤 솔루션이 이러한 요구 사항을 충족할까요?
> 정답 : A

- A. 들어오는 주문을 Amazon Simple Queue Service(Amazon SQS) 대기열로 보냅니다. 각 주문에 대해 마이크로서비스를 오케스트레이션하는 AWS Step Functions 워크플로를 시작합니다. 각 마이크로서비스에 대해 AWS Lambda 함수를 사용합니다.

- B. 수신 주문을 Amazon Simple Notification Service(Amazon SNS) 토픽으로 전송합니다. 각 주문에 대해 마이크로서비스를 오케스트레이션하는 AWS Step Functions 워크플로를 시작합니다. 각 마이크로서비스에 대해 AWS Lambda 함수를 사용합니다.

- C. 수신 주문을 Amazon Simple Queue Service(Amazon SQS) 큐로 보냅니다. Amazon EventBridge를 사용하여 마이크로서비스 간에 이벤트를 분산합니다. 각 마이크로서비스에 대해 AWS Lambda 함수를 사용합니다.

- D. 수신되는 주문을 Amazon Simple Notification Service(Amazon SNS) 토픽으로 전송합니다. Amazon EventBridge를 해당 토픽에 구독하여 마이크로서비스 간에 이벤트를 배포합니다. 각 마이크로서비스에 대해 AWS Lambda 함수를 사용합니다.

---

# Q26. 어떤 회사는 온라인 쇼핑 플랫폼을 AWS로 마이그레이션하면서 서버리스 아키텍처를 도입하려고 합니다.
이 플랫폼에는 정의된 스키마가 없는 사용자 프로필 및 환경 설정 서비스가 있습니다. 이 플랫폼은 사용자 정의 필드를 허용합니다.
프로필 정보는 매일 여러 차례 업데이트됩니다. 회사는 프로필 정보를 내구성 있고 가용성이 높은 솔루션에 저장해야 합니다. 솔루션은 향후 처리를 위해 프로필 데이터의 수정 사항을 포착해야 합니다.
어떤 솔루션이 이러한 요구 사항을 충족시킬까요?
> 정답 : B

- A. Amazon Aurora Serverless v2 클러스터를 사용하여 프로필 데이터를 저장합니다. Amazon CloudWatch Logs의 로그 스트림을 사용하여 수정 사항을 캡처합니다.

- B. Amazon DynamoDB 테이블을 사용하여 프로필 데이터를 저장합니다. Amazon DynamoDB Streams를 사용하여 수정 사항을 캡처합니다.

- C. Amazon ElastiCache(Redis OSS) 클러스터를 사용하여 프로필 데이터를 저장하세요. Amazon Data Firehose를 사용하여 수정 사항을 캡처하세요.

- D. Amazon RDS for PostgreSQL 인스턴스를 사용하여 프로필 데이터를 저장합니다. Amazon CloudWatch Logs의 로그 스트림을 사용하여 수정 사항을 캡처합니다.

---

# Q30. 한 회사가 VPC 내의 Amazon EC2 인스턴스에서 여러 애플리케이션을 실행합니다. 애플리케이션 A는 사용자 지정 라우팅 테이블과 네트워크 ACL이 있는 프라이빗 서브넷에서 실행됩니다. 애플리케이션 B는 동일한 VPC 내의 다른 프라이빗 서브넷에서 실행됩니다.
해당 회사는 애플리케이션 A가 애플리케이션 B로 트래픽을 전송하는 것을 차단해야 합니다.
어떤 솔루션이 이 요구 사항을 충족할까요?
> 정답 : A

- A. 애플리케이션 A 서브넷에 대한 사용자 지정 네트워크 ACL에 아웃바운드 거부 규칙을 추가합니다. 애플리케이션 A가 애플리케이션 B 서브넷과 연결된 IP 주소로 트래픽을 전송하지 못하도록 규칙을 구성합니다.

- B. 애플리케이션 B와 연결된 보안 그룹에 아웃바운드 트래픽 차단 규칙을 추가합니다. 애플리케이션 B가 애플리케이션 A로 트래픽을 전송하지 못하도록 규칙을 구성합니다.

- C. 애플리케이션 B 서브넷에 대한 사용자 지정 네트워크 ACL에 아웃바운드 거부 규칙을 추가합니다. 애플리케이션 B가 애플리케이션 A 서브넷과 연결된 IP 주소로 트래픽을 전송하지 못하도록 규칙을 구성합니다.

- D. 애플리케이션 A와 연결된 보안 그룹에 아웃바운드 트래픽 차단 규칙을 추가합니다. 애플리케이션 A가 애플리케이션 B로 트래픽을 전송하지 못하도록 규칙을 구성합니다.

---

# Q32. 한 회사에 수천 명의 사용자가 있는 웹 애플리케이션이 있습니다. 이 애플리케이션은 사용자가 업로드한 이미지 8~10개를 사용하여 AI 이미지를 생성합니다. 사용자는 생성된 Al 이미지를 6시간마다 한 번씩 다운로드할 수 있습니다. 또한 회사는 생성된 AI 이미지를 사용자가 언제든지 다운로드할 수 있는 프리미엄 사용자 옵션도 제공합니다. 회사는 사용자가 업로드한 이미지를 사용하여 1년에 두 번 AI 모델 훈련을 실행합니다. 회사에는 이미지를 저장할 스토리지 솔루션이 필요합니다.
이러한 요구 사항을 가장 비용 효율적으로 충족하는 스토리지 솔루션은 무엇입니까?
> 정답 : B

- A. 업로드된 이미지를 Amazon S3 One Zone-Infrequent Access로 이동합니다. {S3 One Zone-IA) 프리미엄 사용자 생성 Al 이미지를 S3 Standard로 이동합니다. 프리미엄이 아닌 사용자 생성 Al 이미지를 S3 Standard-Infrequent Access(S3 Standard-IA)로 이동합니다.

- B. 업로드된 이미지를 Amazon S3 Glacier Deep Archive로 이동합니다. 프리미엄 사용자 생성 AI 이미지를 S3 Standard로 이동합니다. 프리미엄이 아닌 사용자 생성 Al 이미지를 S3 Standard-Infrequent Access(S3 Standard-IA)로 이동합니다.

- C. 업로드된 이미지를 Amazon S3 Glacier Deep Archive로 이동합니다. 생성된 모든 AI 이미지를 S3 Glacier 유연한 검색으로 이동합니다.

- D. 업로드된 이미지를 Amazon S3 One Zone-Infrequent Access로 이동합니다. {S3 One Zone-IA) 생성된 모든 Al 이미지를 S3 Glacier 유연한 검색으로 이동합니다.

---

# Q37. 한 회사가 AWS에 내부 웹 애플리케이션을 배포하려고 합니다. 이 웹 애플리케이션은 회사 사무실에서만 접근 가능해야 합니다. 회사는 웹 애플리케이션에 필요한 보안 패치를 인터넷에서 다운로드해야 합니다. 회사는 VPC를 생성하고 회사 사무실과의 AWS 사이트 간 VPN 연결을 구성했습니다. 솔루션 아키텍트는 이 웹 애플리케이션을 위한 안전한 아키텍처를 설계해야 합니다.
어떤 솔루션이 이러한 요구 사항을 충족할까요?
> 정답 : D

- A. 내부 애플리케이션 로드 밸런서(ALB) 뒤의 공용 서브넷에 있는 Amazon EC2 인스턴스에 웹 애플리케이션을 배포합니다. 프라이빗 서브넷에 NAT 게이트웨이를 배포합니다. VPC에 인터넷 게이트웨이를 연결합니다. ALB 보안 그룹의 아웃바운드 대상을 회사 사무실 네트워크 CIDR 블록으로 설정합니다.

- B. 공용 애플리케이션 로드 밸런서(ALB) 뒤의 공용 서브넷에 있는 Amazon EC2 인스턴스에 웹 애플리케이션을 배포합니다. VPC에 인터넷 게이트웨이를 연결합니다. ALB 보안 그룹의 인바운드 소스를 0.0.0.0/0으로 설정합니다.

- C. 공용 애플리케이션 로드 밸런서(ALB) 뒤의 프라이빗 서브넷에 있는 Amazon EC2 인스턴스에 웹 애플리케이션을 배포합니다. VPC에 인터넷 게이트웨이를 연결합니다. ALB의 아웃바운드 대상을 설정합니다.
's 보안 그룹을 0.0.0.0/0으로 설정합니다.

- D. 내부 애플리케이션 로드 밸런서(ALB) 뒤의 프라이빗 서브넷에 있는 Amazon EC2 인스턴스에 웹 애플리케이션을 배포합니다. 퍼블릭 서브넷에 NAT 게이트웨이를 배포합니다. VPC에 인터넷 게이트웨이를 연결합니다.
ALB 보안 그룹의 인바운드 소스를 회사 사무실 네트워크의 CIDR 블록으로 설정하십시오

---

# Q38.한 회사가 단일 Amazon EC2 On-Demand 인스턴스에서 웹사이트 분석 애플리케이션을 호스팅합니다. 분석 애플리케이션은 매우 회복성이 뛰어나고 상태 비저장 모드로 실행되도록 설계되었습니다.
이 회사는 바쁜 시간에 애플리케이션이 성능 저하 징후를 보이고 5xx 오류가 표시된다는 것을 알아차렸습니다. 이 회사는 애플리케이션이 원활하게 확장되도록 해야 합니다.
이러한 요구 사항을 가장 비용 효율적으로 충족할 수 있는 솔루션은 무엇일까요?
> 정답 : A

- A. 웹 애플리케이션의 Amazon Machine Image(AMI)를 만듭니다. AMI를 런치 템플릿에 적용합니다. 런치 템플릿을 포함하는 자동 확장 그룹을 만듭니다. 스팟 플릿을 사용하도록 런치 템플릿을 구성합니다. 자동 확장 그룹에 애플리케이션 로드 밸런서를 연결합니다.

- B. 웹 애플리케이션의 Amazon Machine Image(AMI)를 만듭니다. AMI를 사용하여 두 번째 EC2 On-Demand 인스턴스를 시작합니다. Application Load Balancer를 사용하여 두 EC2 인스턴스에 부하를 분산합니다.

- C. EC2 인스턴스를 중지하고 인스턴스 유형을 변경하기 위한 AWS Lambda 함수를 만듭니다. CPU 사용률이 75%를 넘을 때 Lambda 함수를 호출하기 위한 Amazon CloudWatch 알람을 만듭니다.

- D. 웹 애플리케이션의 Amazon Machine Image(AMI)를 만듭니다. AMI를 사용하여 두 번째 EC2 On-Demand 인스턴스를 시작합니다. Amazon Route 53 가중 라우팅을 사용하여 두 EC2 인스턴스에 부하를 분산합니다.

---

# Q39. 한 회사가 하이브리드 애플리케이션의 가용성과 성능을 개선하고자 합니다. 이 애플리케이션은 다양한 AWS 지역의 Amazon EC2 인스턴스에서 호스팅되는 상태 저장 TCP 기반 워크로드와 온프레미스에서 호스팅되는 상태 없는 UDP 기반 워크로드로 구성됩니다.
솔루션 아키텍트는 가용성과 성능을 개선하기 위해 어떤 작업 조합을 취해야 할까요?
(두 개를 선택하세요.)
> 정답 : A, E

-A. EC2 엔드포인트를 처리하기 위해 각 지역에 네트워크 로드 밸런서를 구성합니다. 온프레미스 엔드포인트로 라우팅하는 각 지역에 네트워크 로드 밸런서를 구성합니다.

- B. 각 지역에 두 개의 애플리케이션 로드 밸런서를 구성합니다. 첫 번째는 EC2 엔드포인트로 라우팅됩니다.
두 번째는 온프레미스 엔드포인트로 라우팅됩니다.

- C. 각 지역에 EC2 엔드포인트를 처리하기 위한 네트워크 로드 밸런서를 구성합니다. 온프레미스 엔드포인트로 라우팅하는 각 지역에 애플리케이션 로드 밸런서를 구성합니다.

- D. Amazon Route 53 지연 기반 라우팅을 사용하여 요청을 로드 밸런서로 라우팅하는 원본이 있는 Amazon CloudFront 배포를 생성합니다.

- E. AWS Global Accelerator를 사용하여 가속기를 만듭니다. 로드 밸런서를 엔드포인트로 추가합니다.


---

# Q41. 한 회사가 Amazon Elastic Container Service(Amazon ECS)에서 워크로드를 실행합니다. ECS 작업 정의가 사용하는 컨테이너 이미지는 Common Vulnerabilities and Exposures(CVE)를 검사해야 합니다.
새로 생성된 컨테이너 이미지도 스캔해야 합니다.
어떤 솔루션이 워크로드를 가장 적게 변경하면서 이러한 요구 사항을 충족할 수 있을까요?
> 정답 : C

- A. 버전 관리가 활성화된 Amazon S3 버킷에 컨테이너 이미지를 저장합니다. s3:ObjectCrealed:* 이벤트에 대한 S3 이벤트 알림을 구성하여 AWS Lambda 함수를 호출합니다. Lambda 함수를 구성하여 Amazon Inspector 스캔을 시작합니다.

- B. 컨테이너 이미지를 Amazon S3 버킷에 저장합니다. Amazon Macie를 사용하여 이미지를 스캔합니다. S3 이벤트 알림을 사용하여 s3:ObjeclCreated:Put 이벤트 유형이 있는 모든 이벤트에 대해 Made 스캔을 시작합니다.

- C. Amazon Elastic Container Registry(Amazon ECR)를 개인 이미지 저장소로 사용하여 컨테이너 이미지를 저장합니다. ECR 기본 스캔에 대해 푸시 필터에서 스캔을 지정합니다.

- D. 워크로드를 Amazon Elastic Kubernetes Service(Amazon EKS)에 배포합니다. Amazon Elastic Container Registry(Amazon ECR)를 개인 이미지 저장소로 사용합니다. ECR 강화 스캔에 대해 푸시 필터에서 스캔을 지정합니다

---

# Q43. 보험 회사가 계약 처리를 위해 사내 애플리케이션을 실행합니다. 이 애플리케이션은 여러 작업으로 구성된 작업을 처리합니다. 개별 작업은 최대 5분 동안 실행됩니다. 일부 작업은 완료하는 데 최대 24시간이 걸릴 수 있습니다. 작업이 실패하면 작업을 다시 처리해야 합니다.
회사는 애플리케이션을 AWS로 마이그레이션하려고 합니다. 회사는 솔루션의 일부로 Amazon S3를 사용할 예정입니다. 회사는 계약이 S3 버킷에 업로드되면 작업이 자동으로 시작되도록 구성하려고 합니다.
어떤 솔루션이 이러한 요구 사항을 충족시킬까요?
> 정답 : A

- A. AWS Step Functions에서 상태 머신을 사용하여 전체 계약 처리 작업을 처리합니다. Amazon EventBridge로 이벤트 알림을 전송하도록 S3 버킷을 구성합니다. Amazon EventBridge에서 상태 머신을 대상으로 하는 규칙을 생성합니다.

- B. AWS Batch 작업을 사용하여 전체 계약 처리 작업을 처리합니다. S3 버킷을 구성하여 Batch 작업을 시작하는 이벤트 알림을 보냅니다.

- C. S3 이벤트 알림을 사용하여 계약이 업로드될 때 Amazon Simple Queue Service(Amazon SQS) 대기열에 알립니다. 대기열에서 메시지를 읽고 계약 처리 작업을 실행하도록 AWS Lambda 함수를 구성합니다.

- D. AWS Lambda 함수를 사용하여 개별 작업을 처리합니다. 개별 Lambda 함수를 순차적으로 호출하여 전체 작업 처리를 담당하는 기본 Lambda 함수를 생성합니다. S3 버킷을 구성하여 기본 Lambda 함수를 호출하여 처리를 시작하도록 이벤트 알림을 보냅니다.


---

# Q47. 한 소셜 미디어 회사가 사용자들에게 웹사이트에 이미지를 업로드할 수 있는 기능을 제공하고 있습니다. 해당 웹사이트는 아마존 EC2 인스턴스에서 운영됩니다. 이미지 업로드 요청 시, 웹사이트는 이미지를 표준 크기로 조정하여 아마존 S3에 저장합니다. 현재 사용자들이 웹사이트 업로드 속도 저하 현상을 겪고 있습니다.
이 회사는 애플리케이션 내 결합도를 낮추고 웹사이트 성능을 개선해야 합니다. 솔루션 아키텍트는 이미지 업로드를 위한 가장 효율적인 운영 프로세스를 설계해야 합니다.
솔루션 설계자는 이러한 요구 사항을 충족하기 위해 어떤 조치들을 조합하여 취해야 할까요? (두 가지를 선택하세요.)
> 정답 : C,D

- A. 이미지를 S3 Glacier Flexible Retrieval에 업로드하도록 애플리케이션을 구성합니다.

- B. 업로드된 이미지의 크기를 조정하기 위해 예약된 시간에 AWS Lambda 함수를 호출하는 Amazon EventBridge 규칙을 생성합니다.

- C. 사전 서명된 URL을 사용하여 각 사용자의 브라우저에서 Amazon S3로 이미지를 직접 업로드하도록 애플리케이션을 구성합니다.

- D. 이미지가 업로드될 때 AWS Lambda 함수를 호출하도록 S3 이벤트 알림을 구성합니다. 해당 함수를 사용하여 이미지 크기를 조정합니다.

- E. 웹 서버가 원본 이미지를 Amazon S3에 업로드하도록 구성합니다.

---

# 48. 한 회사가 AWS Transfer for SFTP 퍼블릭 서버 엔드포인트와 Amazon S3 스토리지를 사용하여 고객을 위한 대규모 데이터 세트를 호스팅합니다. 이 회사는 고객에게 데이터 세트를 인증하고 다운로드할 수 있는 SSH 개인 키를 제공합니다. Transfer for SFTP 서버는 S3 버킷에 저장되는 구조화된 로깅을 사용하도록 구성되어 있습니다. 이 회사는 고객에게 월별 데이터 다운로드 사용량을 기준으로 요금을 청구하려고 합니다. 이러한 요구 사항을 충족하는 솔루션은 무엇입니까?
> 정답 : C

- A. 매달 AWS Cost Explorer를 사용하여 SFTP 전송 비용을 검토하고 고객별 세부 내역을 확인하세요. 해당 요금을 고객의 월별 청구서에 추가하세요.

- B. 소프트웨어를 호스팅하는 S3 버킷에서 요청자 결제를 활성화합니다. 고객의 요청에 따라 각 고객에게 요금을 할당합니다.

- C. 매달 로깅 S3 버킷에 Amazon Athena 쿼리를 실행하여 고객 사용량을 파악하고 비용을 계산합니다. 해당 요금을 고객의 월별 청구서에 추가합니다.

- D. VPC 흐름 로그를 새 S3 버킷에 기록하도록 구성합니다. 흐름 로그에 대해 매월 쿼리를 실행하여 고객 사용량을 파악하고 비용을 계산합니다. 해당 요금을 고객의 월별 청구서에 추가합니다.

---

# Q49. 한 회사에서 JSON 웹 토큰(JWT)을 사용하여 사용자 권한을 부여하는 API를 만들고자 합니다. 또한 경로 기반 라우팅을 사용하여 여러 AWS 서비스에 대한 동적 액세스를 지원해야 합니다.
어떤 솔루션이 이러한 요구 사항을 충족시킬까요?
> 정답 : B

- A. Amazon API Gateway HTTP API 뒤에 네트워크 로드 밸런서를 배포합니다. 권한 부여에는 Amazon Cognito를 사용하세요.

- B. Amazon API Gateway HTTP API 뒤에 애플리케이션 로드 밸런서를 배포합니다. 권한 부여에는 Amazon Cognito를 사용합니다.

- C. Amazon API Gateway REST API 뒤에 네트워크 로드 밸런서를 배포합니다. AWS Lambda 함수를 사용자 지정 권한 부여자로 사용합니다.

- D. Amazon API Gateway REST API 뒤에 애플리케이션 로드 밸런서를 배포합니다. IAM 권한 부여를 구성합니다.

---

# Q51. 한 회사가 민감한 고객 데이터를 처리하는 AWS 기반 클라우드 기반 애플리케이션을 구축하고 있습니다. 이 애플리케이션은 데이터베이스로 Amazon RDS를, 객체 스토리지로 Amazon S3를, 그리고 서버리스 처리를 위해 AWS Lambda를 호출하는 S3 이벤트 알림을 사용합니다.
이 회사는 AWS IAM Identity Center를 사용하여 사용자 자격 증명을 관리합니다. 개발, 테스트 및 운영 팀은 민감한 고객 데이터의 기밀성을 보장하는 동시에 Amazon RDS 및 Amazon S3에 안전하게 액세스할 수 있어야 합니다. 솔루션은 최소 권한 원칙을 준수해야 합니다.
어떤 솔루션이 운영 오버헤드를 최소화하면서 이러한 요구 사항을 충족시킬 수 있을까요?
> 정답 : D

- A. 모든 팀의 각 구성원에게 역할 기반 권한을 부여하는 개별 IAM 사용자를 생성합니다. 사용자 요구에 따라 RDS 및 S3 접근 권한에 대한 사전 정의된 정책을 사용하여 각 사용자에게 IAM 역할을 할당합니다. 정기적인 자격 증명 평가를 위해 IAM Access Analyzer를 구현합니다.

- B. AWS Organizations를 사용하여 각 팀에 대해 별도의 계정을 생성하세요. 최소 권한으로 교차 계정 IAM 역할을 구현하세요. 팀 역할 및 책임에 따라 RDS 및 S3 액세스에 대한 특정 권한을 부여하세요.

- C. 모든 팀에 최소 권한의 IAM 역할을 사용하여 액세스 권한을 부여합니다. 팀의 책임에 따라 Amazon RDS 및 S3 객체 액세스에 대한 특정 권한을 정의하는 맞춤형 IAM 정책을 사용하여 각 팀에 IAM 역할을 할당합니다.

- D. Identity Center 디렉터리를 사용하여 IAM Identity Center를 활성화합니다. Amazon RDS 및 Amazon S3에 대한 세부적인 접근 권한을 가진 권한 집합을 생성하고 구성합니다. 모든 팀을 해당 권한 집합을 통해 특정 접근 권한을 가진 그룹에 할당합니다.

---

# Q54. 한 회사가 여러 리더보드, 플레이어 매치메이킹 시스템, 인증 서비스에 동시에 고유 이벤트를 전송해야 하는 게임 애플리케이션을 구축하고 있습니다. 이 회사는 이벤트를 순서대로 전달하고 게시-구독 모델을 지원하는 AWS 기반 이벤트 기반 시스템이 필요합니다. 게임 애플리케이션은 게시자여야 하고, 리더보드, 매치메이킹 시스템, 인증 서비스는 구독자여야 합니다.
어떤 솔루션이 이러한 요구 사항을 충족시킬까요?
> 정답 : A

- A. Amazon Simple Notification Service(Amazon SNS) FIFO 주제

- B. Amazon Simple Queue Service(Amazon SQS) FIFO 대기열

- C. Amazon Simple Notification Service(Amazon SNS) 표준 주제

- D. Amazon EventBridge 이벤트 버스

---

# Q55. 한 회사가 단기 프로젝트를 위해 계약직 팀에게 회사의 AWS 리소스에 대한 임시 접근 권한을 제공해야 합니다. 계약직 팀원들은 AWS 서비스에 대해 서로 다른 수준의 접근 권한이 필요합니다. 회사는 프로젝트가 완료되면 모든 계약직 팀의 권한을 취소해야 합니다.
어떤 솔루션이 운영 비용을 최소화하면서 이러한 요구 사항을 충족할 수 있을까요?
> 정답 : C

- A. 계약자의 접근 패턴을 모니터링하도록 AWS Config 규칙을 구성하세요. AWS Config 규칙을 사용하여 사용하지 않거나 권한이 너무 많은 권한을 자동으로 취소하세요.

- B. AWS CloudTrail과 맞춤형 Amazon EventBridge 트리거를 사용하여 계약자의 작업을 감사합니다. 활동 로그를 기반으로 각 계약자의 권한을 조정합니다.

- C. AWS 보안 토큰 서비스(AWS STS)를 사용하여 계약자를 위한 임시 자격 증명을 생성합니다. 미리 정의된 역할에 따라 계약자에게 액세스 권한을 제공합니다. 프로젝트가 완료되면 액세스 권한이 자동으로 만료되도록 설정합니다.

- D. AWS IAM을 사용하여 각 계약자용 사용자 계정을 생성하세요. 계약자의 액세스 수준을 정의하는 정책을 사용자 계정에 연결하세요. 프로젝트가 완료되면 계정을 수동으로 비활성화하세요

---

# Q56. 한 전자상거래 회사는 매주 월요일 오전 8시, 주간 할인 행사 기간 동안 모바일 애플리케이션 트래픽이 급증합니다. 해당 애플리케이션의 백엔드는 Amazon API Gateway HTTP API와 AWS Lambda 함수를 사용하여 사용자 요청을 처리합니다. 특히 할인 행사가 집중되는 시간대에는 Lambda 함수에서 TooManyRequestsException 오류가 발생하는 문제가 보고되고 있으며, 이로 인해 사용자 경험이 저하됩니다. 솔루션 아키텍트는 오류를 최소화하고 애플리케이션의 전반적인 기능에 영향을 미치지 않으면서 확장 가능하고 복원력이 뛰어난 솔루션을 설계해야 합니다.
> 정답 : A

- A. Amazon Simple Queue Service(Amazon SQS) 대기열을 생성합니다. 사용자 요청을 SQS 대기열로 보냅니다.
프로비저닝된 동시 실행 수로 Lambda 함수를 구성합니다. 이벤트 소스 트리거로 SQS 큐를 설정합니다.

- B. Amazon Simple Queue Service(Amazon SQS) 대기열을 생성합니다. 사용자 요청을 SQS 대기열로 보냅니다.
Lambda 함수를 예약된 동시 실행으로 구성합니다. 함수의 이벤트 소스 트리거로 SQS 큐를 설정합니다.

- C. Amazon Simple Notification Service(Amazon SNS) 토픽을 생성합니다. SNS 토픽으로 사용자 요청을 보냅니다. 프로비저닝된 동시 실행 수로 Lambda 함수를 구성합니다. 함수를 SNS 토픽에 구독합니다.

- D. AWS Step Functions를 사용하여 사용자 요청을 오케스트레이션하고 처리합니다. Step Functions를 구성하여 Lambda 함수를 호출하고 요청 흐름을 관리합니다.


---

# Q57. 한 회사가 아마존 EC2 인스턴스에서 실행될 마이크로소프트 윈도우 기반의 단일체 애플리케이션을 개발 중입니다. 이 애플리케이션은 중단 없이 장시간 데이터 처리 작업을 수행해야 합니다. 회사는 향후 3년간의 예상 사용량 증가를 모델링했으며, 이 3년의 증가 기간 동안 EC2 인스턴스 비용을 최적화하고자 합니다.
> 정답 : D

- A. 1년 약정으로 컴퓨팅 절약 플랜을 구매하세요. 필요에 따라 매년 구매를 갱신하고 용량을 조정하세요.

- B. 3년 약정으로 EC2 인스턴스 절약 플랜을 구매하세요. 플랜 권장 사항에 따라 시간당 약정량을 조정하세요.

- C. EC2 스팟 인스턴스에 애플리케이션을 배포합니다. 애플리케이션이 항상 실행되도록 최소 크기가 1인 자동 스케일링 그룹을 사용합니다.

- D. 3년 약정으로 컴퓨팅 절약 플랜을 구매하세요. 플랜 권장 사항에 따라 시간당 약정량을 조정하세요.

---

# Q58. 한 회사가 지능형 매트리스에서 밤새 수면을 추적하기 위해 데이터를 수집하는 새로운 장치를 출시하려고 합니다. 센서는 Amazon S3 버킷에 업로드될 데이터를 전송합니다. 각 매트리스는 매일 밤 약 2MB의 데이터를 생성합니다.
애플리케이션은 각 사용자의 데이터를 처리하고 요약해야 합니다. 애플리케이션은 가능한 한 빨리 결과를 제공해야 합니다. 애플리케이션을 실행할 때마다 약 1GB의 메모리가 필요하며 30초 이내에 실행이 완료됩니다.
어떤 솔루션이 애플리케이션을 가장 비용 효율적으로 실행할 수 있을까요?
> 정답 : B

- A. Scala 작업이 포함된 AWS Glue

- B. Python 스크립트를 사용한 AWS Lambda

- C. PySpark 작업이 포함된 AWS Glue

- D. Apache Spark 스크립트를 사용한 Amazon EMR

---

# Q추가1. 한 전자상거래 회사가 AWS에서 쇼핑몰 애플리케이션을 운영하고 있습니다. 이 애플리케이션은 Amazon Aurora PostgreSQL 클러스터를 기본 데이터베이스로 사용하며 다중 AZ(Multi-AZ) 구성되어 있습니다.
최근 대규모 할인 행사 기간 동안 데이터베이스에 과도한 읽기 및 쓰기 트래픽이 몰려 애플리케이션 연결 시간 초과(Timeout) 가 발생했습니다.
다운타임을 최소화하면서 애플리케이션의 확장성과 가용성을 높이는 솔루션은 무엇입니까?
> 정답 : C

- A. Aurora 클러스터의 상태 변경을 감지하는 Amazon EventBridge 규칙을 생성하고 AWS Lambda 함수로 대응합니다.

- B. Aurora 클러스터를 수정하여 제로 다운타임 패치(Zero Downtime Patching) 기능을 활성화합니다.

- C. Aurora 클러스터에 읽기 전용 복제본(Read Replica) 을 추가하고, 애플리케이션과 데이터베이스 사이에 Amazon RDS Proxy를 배포합니다.

- D. Amazon ElastiCache for Redis를 도입하고, AWS Database Migration Service(AWS DMS) 를 사용하여 데이터를 동기화합니다.

---

# Q추가2. 한 회사의 비즈니스 시스템은 매일 수백 개의 CSV 보고서를 생성하여 로컬 네트워크 공유 폴더에 저장합니다.
분석 팀은 이 데이터를 생성 직후(Near Real-time) AWS 클라우드로 전송하여 분석에 활용하고자 합니다.
운영 오버헤드를 최소화하면서 이를 구현하는 솔루션은 무엇입니까?
> 정답 : B

- A. 매일 업무 종료 시 실행되도록 AWS DataSync 예약 작업을 구성합니다.

- B. Amazon S3 File Gateway를 배포하고, 비즈니스 시스템이 해당 파일 공유에 데이터를 저장하도록 구성합니다.

- C. 파일 생성을 감지하여 AWS DataSync API를 호출하는 애플리케이션을 개발합니다.

- D. SFTP용 AWS Transfer Family 엔드포인트를 배포하고 스크립트를 통해 업로드합니다.

---

# Q추가3. 한 글로벌 물류 회사가 여러 파트너사로부터 배송 데이터를 수집하는 온프레미스 시스템을 운영 중입니다.
이 회사는 AWS로 인프라를 이전하면서 Amazon S3 API를 통해 데이터를 직접 업로드하는 새로운 애플리케이션을 구축했습니다.
그러나 일부 레거시 시스템을 사용하는 파트너사들은 여전히 SFTP(SSH File Transfer Protocol)를 통해서만 데이터를 전송할 수 있습니다.
회사는 서버를 직접 관리하지 않으면서 이러한 파트너사들이 S3 버킷으로 데이터를 안전하게 전송할 수 있도록 지원하고자 합니다.
운영 오버헤드를 최소화하면서 요구 사항을 충족하는 솔루션은 무엇입니까?
> 정답 : B

- A. 파트너사의 온프레미스 스토리지에서 Amazon S3로 데이터를 복제하도록 AWS DataSync 에이전트를 설치 및 구성합니다.

- B. 레거시 파트너사를 위해 AWS Transfer Family 서버를 생성하고 SFTP 프로토콜을 활성화하여 Amazon S3 버킷과 연결합니다.

- C. Amazon EC2 인스턴스를 프로비저닝하고 SFTP 서버 소프트웨어를 설치한 후, 파트너사들이 이 인스턴스로 데이터를 업로드하도록 구성합니다.

- D. 레거시 파트너사를 위해 Amazon S3 File Gateway를 배포하고, 이를 통해 데이터를 업로드하도록 SMB 파일 공유를 구성합니다.

---

# Q추가4. 한 회사가 Amazon RDS for MySQL을 사용하여 프로덕션 데이터베이스를 운영하고 있습니다. 이 회사는 데이터베이스에 저장된 최신 데이터를 자주 조회하지만, 동일한 쿼리를 반복하지는 않는 새로운 분석 애플리케이션을 구축하려고 합니다. 이 새로운 애플리케이션이 프로덕션 데이터베이스의 성능에 영향을 주지 않도록 해야 합니다. 이를 위한 최적의 솔루션은 무엇입니까?
> 정답 : C

- A. Amazon ElastiCache 클러스터를 생성하여 쿼리 결과를 캐싱합니다.

- B. Application Load Balancer(ALB)를 설정하여 트래픽을 제어합니다.

- C. 데이터베이스에 대한 읽기 전용 복제본(Read Replica) 을 생성하고, 분석 애플리케이션이 이를 바라보도록 합니다.

- D. 데이터베이스 스냅샷을 생성하고, 애플리케이션이 스냅샷을 쿼리하도록 합니다.

---

# Q추가5. 한 회사가 기존 온프레미스 Oracle 데이터베이스에서 관리하던 고해상도 지리 정보 시스템(GIS) 데이터를 AWS로 이전하려고 합니다. 이 데이터베이스는 단일 테이블 내에 수백만 개의 고해상도 이미지를 포함하고 있으며, 각 이미지는 고유한 지리 코드로 식별됩니다. 특히 자연재해와 같은 비상 상황 시에는 수만 개의 이미지가 몇 분 단위로 급격히 업데이트되는 특성이 있습니다. 회사는 이러한 대규모 트래픽 급증 상황에서도 데이터 가용성을 보장하고 유연하게 확장할 수 있는 아키텍처를 설계하고자 합니다. 가장 적합한 솔루션은 무엇입니까?
> 정답 : B

- A. 기존과 동일하게 이미지와 지리 코드를 데이터베이스 내부에 저장합니다. 고가용성을 위해 Amazon RDS for Oracle Multi-AZ 배포를 활용합니다.
- B. 이미지는 확장성이 뛰어난 Amazon S3 버킷에 저장합니다. 메타데이터 관리를 위해 Amazon DynamoDB를 사용하여 지리 코드를 키로, 이미지의 S3 URL을 값으로 저장합니다.
- C. 이미지 파일 자체를 지리 코드와 함께 Amazon DynamoDB 테이블에 직접 저장합니다. 읽기 부하를 분산하기 위해 DynamoDB Accelerator(DAX)를 추가로 구성합니다.
- D. 이미지는 Amazon S3 버킷에 보관합니다. 지리 코드와 해당 이미지의 S3 URL 정보는 관리형 관계형 데이터베이스인 Amazon RDS의 테이블에 저장하여 관리합니다.

---

# Q추가6. 한 글로벌 물류 회사가 Amazon RDS for MySQL을 사용하여 핵심 배송 추적 애플리케이션을 운영하고 있습니다. 이 애플리케이션은 24시간 중단 없는 고가용성이 필수적이며, 장애 발생 시 자동으로 복구되어야 합니다. 또한, 비즈니스 분석 팀은 운영 데이터베이스에서 대규모 배치(Batch) 분석 쿼리를 실행하여 3시간 이내의 지연 시간을 허용하는 보고서를 생성해야 합니다. 이 분석 작업이 실시간 배송 트랜잭션 처리의 성능을 저하시켜서는 안 됩니다. 이러한 요구 사항을 충족하는 아키텍처는 무엇입니까?
> 정답 : C

- A. Amazon RDS for MySQL을 Multi-AZ DB 인스턴스로 구성합니다. 트랜잭션 쿼리는 기본(Primary) 인스턴스로 보내고, 분석 쿼리는 다른 가용 영역에 있는 대기(Standby) 인스턴스로 보내도록 설정합니다.
- B. Amazon RDS for MySQL 데이터베이스에 대해 자동 백업을 활성화합니다. 매일 밤 최신 스냅샷을 사용하여 분석용 읽기 전용 데이터베이스를 복원하고, 분석 작업이 끝나면 해당 데이터베이스를 종료합니다.
- C. Amazon RDS for MySQL에 대해 Multi-AZ 배포를 활성화하고, 다른 가용 영역에 하나 이상의 읽기 전용 복제본(Read Replica)을 생성합니다. 트랜잭션 트래픽은 기본 DB 인스턴스로, 분석 트래픽은 읽기 전용 복제본으로 라우팅합니다.
- D. Amazon RDS for MySQL Multi-AZ DB 클러스터를 배포하고 두 개의 대기 인스턴스를 구성합니다. 트랜잭션 트래픽은 기본 인스턴스에서 처리하고, 분석 쿼리는 리더 엔드포인트(Reader Endpoint)를 통해 부하를 분산합니다.

---

# Q추가7. 한 회사가 AWS Lambda 함수를 사용하여 Amazon RDS for MySQL 데이터베이스 인스턴스에 연결하는 애플리케이션을 구축하고 있습니다. 데이터베이스는 다수의 동시 연결을 처리해야 하며, 이로 인해 리소스 부하가 발생하고 있습니다. 회사는 데이터베이스 연결 효율성을 높이고 장애 조치(Failover) 시 복구 시간을 단축하고자 합니다. 운영 비용을 최소화하면서 이러한 요구 사항을 충족하는 솔루션은 무엇입니까?
> 정답 : A

- A. 데이터베이스 연결 풀링(Connection Pooling)을 처리하기 위해 Amazon RDS Proxy를 구현하고, 애플리케이션이 프록시 엔드포인트를 사용하도록 수정합니다.
- B. 연결 부하를 분산하기 위해 새로운 RDS 인스턴스를 추가로 생성하고 애플리케이션이 새 인스턴스를 가리키도록 수정합니다.
- C. 데이터베이스 부하를 분산하기 위해 읽기 전용 복제본(Read Replica)을 생성하고 네트워크 로드 밸런서(NLB)를 사용하여 트래픽을 분산합니다.
- D. 데이터베이스 성능을 향상시키기 위해 기존 RDS for MySQL 인스턴스를 Amazon Aurora MySQL로 마이그레이션합니다.

---

# Q추가8. 한 전자상거래 회사가 수백만 개의 제품 정보를 처리하는 카탈로그 시스템을 구축하고 있습니다. 시스템은 제품명, 가격, 카테고리와 같은 정형 데이터와 고해상도 제품 영상, 매뉴얼 같은 비정형 데이터를 모두 저장해야 합니다. 대규모 할인 행사 시 트래픽 폭주에도 견딜 수 있는 고가용성 아키텍처가 필요합니다. 가장 적절한 아키텍처는 무엇입니까?
> 정답 : B

- A. 제품 정보는 Amazon RDS Multi-AZ에, 영상과 매뉴얼은 Amazon S3에 저장합니다.
- B. 제품 정보는 Amazon DynamoDB에, 영상과 매뉴얼은 Amazon S3에 저장합니다.
- C. 모든 데이터를 Amazon DynamoDB에 저장합니다.
- D. 모든 데이터를 Amazon DocumentDB에 저장합니다.

---

# Q59. 미디어 회사가 비디오를 업로드하기 위해 AWS에 웹 애플리케이션을 호스팅합니다. 인증된 사용자만 인증 후 지정된 시간 내에 업로드해야 합니다.
어떤 솔루션이 운영 비용을 최소화하면서 이러한 요구 사항을 충족할 수 있을까요?
> 정답 : B

- A. 애플리케이션을 통한 S3 버킷 직접 액세스를 제어하고 기록하기 위해 Amazon Cognito와 통합되는 사용자 정의 인증 서비스를 개발합니다.

- B. 사용자가 인증될 때 미리 서명된 URL을 생성하는 AWS Lambda 함수를 만듭니다.

- C. 인증된 사용자에 대한 IAM 임시 보안 자격 증명을 생성하도록 애플리케이션을 구성합니다.

- D. AWS 보안 토큰 서비스(AWS STS)를 사용해 인증된 사용자에게 비디오를 S3 버킷에 직접 업로드할 수 있는 임시 권한을 부여하는 사전 정의된 IAM 역할을 맡습니다.

---

# Q60. 한 회사가 온프레미스 데이터 센터에서 AWS 클라우드로 2계층 애플리케이션을 마이그레이션했습니다. 데이터 계층은 12TB의 범용 SSD Amazon EBS 스토리지를 사용하는 Multi-AZ 배포 방식의 Amazon RDS for Oracle입니다. 이 애플리케이션은 평균 문서 크기가 6MB인 바이너리 대형 객체(BLOB) 형태로 데이터베이스에 문서를 처리하고 저장하도록 설계되었습니다.
데이터베이스 크기가 시간이 지남에 따라 증가하여 성능이 저하되고 저장 비용이 증가했습니다. 회사는 데이터베이스 성능을 개선해야 하며, 이를 위해 고가용성 및 복원력이 뛰어난 솔루션이 필요합니다.
어떤 솔루션이 이러한 요구 사항을 가장 비용 효율적으로 충족할까요?
> 정답 : A

- A. Amazon S3 버킷을 생성합니다. 애플리케이션을 업데이트하여 문서를 S3 버킷에 저장하도록 합니다. 객체 메타데이터는 기존 데이터베이스에 저장합니다.

- B. RDS DB 인스턴스 크기를 늘립니다. 저장 용량을 24TiB로 늘립니다. 저장 유형을 프로비저닝된 IOPS로 변경합니다.

- C. RDS DB 인스턴스 크기를 줄입니다. 저장 용량을 24TiB로 늘립니다. 저장 유형을 마그네틱으로 변경합니다.

- D. Amazon DynamoDB 테이블을 생성합니다. DynamoDB를 사용하도록 애플리케이션을 업데이트합니다. AWS DMS를 사용하여 Oracle 데이터베이스에서 DynamoDB로 데이터를 마이그레이션합니다.

---

# Q63. 한 회사가 Microsoft SQL Server 데이터베이스를 사용하고 있습니다. 현재 애플리케이션은 SQL Server 프로토콜을 사용하여 데이터베이스에 연결합니다. 이 회사는 애플리케이션 코드 변경을 최소화하면서 Amazon Aurora PostgreSQL로 마이그레이션하기를 원합니다.
다음 요구 사항을 충족하는 단계 조합은 무엇입니까? (두 가지를 선택하십시오.)
> 정답 : B

- A. AWS DMS를 사용하여 애플리케이션의 SQL 쿼리를 다시 작성합니다.

- B. Aurora PostgreSQL에서 Babelfish를 활성화하여 SQL Server 쿼리를 실행할 수 있습니다.

- C. Amazon RDS Proxy를 사용하여 애플리케이션을 Aurora PostgreSQL에 연결합니다.

- D. AWS SCT 및 AWS DMS를 사용하여 데이터베이스 스키마와 데이터를 마이그레이션합니다.

- E. AWS SCT를 사용하여 애플리케이션의 SQL 쿼리를 다시 작성합니다.

---

# Q69. 솔루션 아키텍트가 한 회사의 다중 리전 재해 복구(DR) 전략을 설계하고 있습니다. 이 회사는 애플리케이션 로드 밸런서(ALB) 뒤에 있는 Auto Scaling 그룹의 Amazon EC2 인스턴스에서 애플리케이션을 실행합니다. 이 애플리케이션은 회사의 기본 및 보조 AWS 리전에 호스팅됩니다.
기본 리전에 장애가 발생할 경우, 애플리케이션은 보조 리전의 DNS 쿼리에 응답해야 합니다. 한 번에 하나의 리전만 트래픽을 처리해야 합니다.
어떤 솔루션이 이러한 요구 사항을 충족시킬까요?
> 정답 : C

- A. Amazon Route 53에서 트래픽 정책을 생성합니다. 지리적 위치 라우팅 정책과 ELB 애플리케이션 부하 분산 장치의 값 유형을 사용합니다.

- B. Amazon Route 53 Resolver에서 아웃바운드 엔드포인트를 생성합니다. 네트워크의 DNS 확인자로 쿼리가 전달되는 방식을 결정하는 전달 규칙을 생성합니다. 각 리전의 VPC에 규칙을 연결합니다.

- C. Amazon Route 53에서 기본 및 보조 DNS 레코드를 생성합니다. 상태 검사 및 장애 조치 라우팅 정책을 구성합니다.

- D. Amazon Route 53 프로필을 생성합니다. DNS 리소스를 프로필에 연결합니다. 프로필을 각 리전의 VPC에 연결합니다.

--- 

# Q74. 한 의료 회사가 개인 식별 정보(PII)를 Amazon RDS for Oracle 데이터베이스에 저장합니다. 이 회사는 저장된 PII 데이터를 암호화해야 하며, 암호화 키를 저장하고 관리하기 위해 전용 하드웨어 모듈을 사용해야 합니다.
> 정답 : D

- A. AWS 키 관리 서비스(AWS KMS)를 사용하여 RDS 데이터베이스에 대한 암호화를 구성합니다. 키는 AWS CloudHSM에 저장하고 관리합니다.

- B. RDS 스냅샷을 구성하고 Amazon S3 관리형 키(SSE-S3)를 사용하여 서버 측 암호화를 사용합니다.
AWS CloudHSM에 키를 저장하세요.

- C. AWS CloudHSM 기반 AWS KMS 키를 사용하여 RDS 데이터베이스에 대한 투명 암호화를 구성합니다.

- D. AWS CloudHSM 기반 키를 사용하여 데이터베이스 볼륨을 암호화하려면 Amazon EC2 인스턴스 스토어 암호화를 사용하십시오.

---

# Q77. 한 회사가 직원의 노트북에서 파이썬 스크립트를 실행하여 Amazon RDS 데이터베이스에 저장된 데이터를 변환하고 업데이트합니다. 이 회사는 데이터 변환 워크로드를 AWS 클라우드로 마이그레이션하려고 합니다. 솔루션은 30분마다 파이썬 스크립트를 실행해야 하며, 각 실행마다 RDS 데이터베이스의 데이터를 업데이트하는 데 약 20초가 소요됩니다.
어떤 솔루션이 이러한 요구 사항을 가장 비용 효율적으로 충족할까요?
> 정답 : B

- A. Python 스크립트를 실행할 Amazon EC2 스팟 인스턴스를 시작합니다. EC2 인스턴스가 RDS 데이터베이스에 액세스할 수 있도록 구성합니다. Python 스크립트를 실행하는 cron 작업을 구성합니다.

- B. RDS 데이터베이스에 접근하도록 AWS Lambda 함수를 구성합니다. Amazon EventBridge 스케줄러를 사용하여 Lambda 함수를 호출합니다.

- C. Python 스크립트를 실행할 Amazon EC2 온디맨드 인스턴스를 시작합니다. EC2 인스턴스가 RDS 데이터베이스에 액세스할 수 있도록 구성합니다. Python 스크립트를 실행하는 cron 작업을 구성합니다.

- D. RDS 데이터베이스에 액세스하도록 AWS Lambda 함수를 구성합니다. 프로비저닝된 동시 실행 수를 사용하여 Lambda 함수를 구성합니다. Amazon API Gateway HTTP API를 사용하여 Lambda 함수를 호출합니다.

---

# Q79. 한 회사가 Amazon EC2 인스턴스가 포함된 개발 계정을 보유하고 있습니다. 회사는 이 EC2 인스턴스를 테스트 용도로 사용합니다. 최근 개발 계정에 대한 감사 결과, 일부 개발자가 테스트 완료 후 인스턴스를 중지하는 것을 간혹 잊어버리는 경우가 있어 추가 비용이 발생하는 것으로 나타났습니다. 회사는 개발 계정의 비용을 최적화하고자 하며, AWS Budgets를 사용하여 계정에 대한 예산을 설정하려고 합니다.
어떤 솔루션이 이러한 요구 사항을 충족할까요?
> 정답 : C

- A. 예산 임계값이 예산 금액의 100%에 도달할 때 AWS Budgets에서 알림을 정의합니다.
Amazon EventBridge 예약 규칙을 생성합니다. 예약 규칙에 따라 EC2 인스턴스를 중지하는 AWS Lambda 함수를 구현합니다.

- B. 예산 임계값이 예상 비용의 100%에 도달할 때 AWS Budgets에서 알림을 정의합니다.
알림에 EC2 인스턴스를 자동으로 중지하는 작업을 구현하세요.

- C. 예산 임계값이 예산 금액의 100%에 도달할 때 AWS Budgets에서 알림을 정의합니다.
알림에 EC2 인스턴스를 자동으로 중지하는 작업을 구현하세요.

- D. 예산 임계값이 예상 비용의 100%에 도달할 때 AWS Budgets에서 알림을 정의합니다.
AWS Budgets를 구성하여 Amazon SNS 알림을 AWS Lambda 함수로 전송합니다. Lambda 함수가 알림을 수신하면 EC2 인스턴스를 중지하도록 구성합니다.

---

# Q81. 한 회사가 AWS에서 애플리케이션을 호스팅합니다. 이 애플리케이션은 사용자에게 사진을 업로드하고 Amazon S3 버킷에 사진을 저장할 수 있는 기능을 제공합니다. 이 회사는 Amazon CloudFront와 사용자 지정 도메인 이름을 사용하여 eu-west-1 지역의 S3 버킷에 사진 파일을 업로드하려고 합니다.
이러한 요구 사항을 충족하는 솔루션은 무엇입니까? (두 개를 선택하세요.)
> 정답 : B,E

- A. CloudFront에서 업로드를 허용하도록 Amazon S3를 구성합니다. S3 전송 가속을 구성합니다.

- B. CloudFront 원본 액세스 제어(OAC)에서 업로드를 허용하도록 Amazon S3를 구성합니다.

- C. CloudFront에서 업로드를 허용하도록 Amazon S3를 구성합니다. Amazon S3 웹사이트 엔드포인트를 구성합니다.

- D. AWS Certificate Manager(ACM)를 사용하여 eu-west-1에 공개 인증서를 만듭니다. CloudFront에서 인증서를 사용합니다.

- E. AWS Certificate Manager(ACM)를 사용하여 us-east-1 지역에서 퍼블릭 인증서를 만듭니다. CloudFront에서 인증서를 사용합니다.

---

# Q85. 한 의료 회사가 환자 데이터 처리를 위해 Amazon EMR 클러스터를 사용합니다. 데이터는 전송 중과 저장 시 모두 암호화되어야 하며, 클러스터 내의 로컬 볼륨 또한 암호화되어야 합니다. 이러한 요구 사항을 충족하는 솔루션은 무엇일까요?
> 정답 : A

- A. 필요에 따라 데이터와 볼륨을 암호화하는 EMR 보안 구성을 생성합니다.

- B. EMR 인스턴스용 EC2 인스턴스 프로필을 생성합니다. 암호화를 적용하도록 인스턴스 프로필을 구성합니다.

- C. EMR 클러스터에 대한 신뢰 정책이 있는 런타임 역할을 생성합니다.

- D. Amazon EBS 볼륨을 생성합니다. 암호화를 활성화합니다. 볼륨을 기존 EMR 클러스터에 연결합니다.

---

# Q92. 한 회사에 직원이 15명 있습니다. 이 회사는 직원의 근무 시작일을 Amazon DynamoDB 테이블에 저장합니다. 이 회사는 직원의 근무 기념일에 각 직원에게 이메일 메시지를 보내려고 합니다.
이러한 요구 사항을 충족하면서 가장 운영 효율적인 솔루션은 무엇일까요?
> 정답 : B

- A. DynamoDB 테이블을 스캔하고 필요할 때 Amazon Simple Notification Service(Amazon SNS)를 사용하여 직원에게 이메일 메시지를 보내는 스크립트를 만듭니다. cron 작업을 사용하여 Amazon EC2 인스턴스에서 매일 이 스크립트를 실행합니다.

- B. DynamoDB 테이블을 스캔하고 Amazon Simple Notification Service(Amazon SNS)를 사용하여 필요할 때 직원에게 이메일 메시지를 보내는 AWS Lambda 함수를 만듭니다. 이 Lambda 함수를 매일 실행되도록 예약합니다.

- C. DynamoDB 테이블을 스캔하고 Amazon Simple Queue Service {Amazon SQS}를 사용하여 필요할 때 직원에게 이메일 메시지를 보내는 스크립트를 만듭니다. cron 작업을 사용하여 이 스크립트를 Amazon EC2 인스턴스에서 매일 실행합니다.

- D. DynamoDB 테이블을 스캔하고 필요할 때 Amazon Simple Queue Service(Amazon SQS)를 사용하여 직원에게 이메일 메시지를 전송하는 AWS Lambda 함수를 만듭니다. 이 Lambda 함수가 매일 실행되도록 예약합니다.

---

# Q93. 개발자는 AWS SDK를 사용하여 10개 서비스에 대한 로그 레코드를 집계하고 생성하는 애플리케이션을 만들었습니다. 이 애플리케이션은 Amazon Kinesis Data Streams 스트림에 데이터를 전달합니다. 각 레코드에는 서비스 이름, 생성 타임스탬프 및 기타 로그 정보가 포함된 로그 메시지가 들어 있습니다. 스트림에는 프로비저닝된 용량 모드에서 15개의 샤드가 있습니다. 스트림은 서비스 이름을 파티션 키로 사용합니다.
개발자는 모든 서비스가 로그를 생성할 때 다음 사항을 확인합니다.  ProvisionedThroughputExceededException 오류는 PutRecord 요청 중에 발생합니다. 스트림 메트릭은 애플리케이션이 사용하는 쓰기 용량이 프로비저닝된 용량보다 낮음을 보여줍니다. 개발자는 이 문제를 어떻게 해결해야 할까요?
> 정답 : D

- A. 각 서비스마다 별도의 Kinesis 스트림을 사용하여 로그를 생성합니다.

- B. 조절 오류가 발생하지 않을 때까지 샤드 수를 두 배로 늘립니다.

- C. 용량 모드를 프로비저닝에서 주문형으로 변경합니다.

- D. 파티션 키를 서비스 이름에서 생성 타임스탬프로 변경합니다.

---

# Q94. 어떤 회사에서는 높은 IOPS가 필요한 HPC 워크로드를 실행합니다. 이러한 요구 사항을 충족하는 단계 조합은 무엇입니까? (두 가지 선택)
> 정답 : A, C

- A.Amazon FSx for Lustre를 고성능 파일 시스템으로 사용하세요.

- B.EC2 인스턴스의 자동 확장 그룹을 만듭니다. 예약된 인스턴스를 사용합니다. 스프레드 배치 그룹을 구성합니다. 분석을 위해 AWS Batch를 사용합니다.

- C.EC2 인스턴스의 자동 확장 그룹을 만듭니다. 혼합 인스턴스 유형과 클러스터 배치 그룹을 사용합니다. 분석에는 Amazon EMR을 사용합니다.

- D.Amazon S3에 Mountpoint를 고성능 파일 시스템으로 사용하세요.

- E.Amazon EFS를 고성능 파일 시스템으로 사용하세요.

---

# Q100. 한 회사가 최근 AWS의 VPC로 애플리케이션을 마이그레이션했습니다. AWS Site-to-Site VPN 연결은 회사의 온프레미스 네트워크를 VPC에 연결합니다. 애플리케이션은 온프레미스에 있는 다른 시스템에서 고객 데이터를 검색합니다. 애플리케이션은 온프레미스 DNS 서버를 사용하여 도메인 레코드를 확인합니다. 마이그레이션 후 이름 확인 오류로 인해 애플리케이션이 고객 데이터에 연결할 수 없습니다.
어떤 솔루션을 사용하면 애플리케이션이 내부 도메인 이름을 확인할 수 있을까요?
> 정답 : A

- A. Amazon Route 53 Resolver 아웃바운드 엔드포인트를 생성합니다. 온프레미스 도메인에 대한 DNS 쿼리를 온프레미스 DNS 서버로 전달하도록 아웃바운드 엔드포인트를 구성합니다.

- B. AWS 환경과 온프레미스 네트워크 간에 두 개의 AWS Direct Connect 연결을 설정합니다. 두 연결을 포함하는 링크 집계 그룹(LAG)을 설정합니다. VPC 확인자 주소가 온프레미스 DNS 서버를 가리키도록 변경합니다.

- C. VPC에서 EC2 인스턴스를 시작합니다. EC2 인스턴스에서 모든 DNS 요청을 온프레미스 DNS 서버로 전달하는 사용자 지정 DNS 전달자를 배포합니다. EC2 인스턴스를 네임 서버로 사용하는 Amazon Route 53 프라이빗 호스팅 영역을 생성합니다.

- D. 온프레미스 도메인에 대한 Amazon Route 53 퍼블릭 호스팅 영역을 생성합니다. 온프레미스 도메인에 대한 DNS 요청을 Route 53 퍼블릭 호스팅 영역으로 전달하도록 네트워크 ACL을 구성합니다.


---

# Q108. 분석 애플리케이션이 Amazon Elastic File System(Amazon EFS) 표준 스토리지를 사용하는 여러 Amazon EC2 Linux 인스턴스에서 실행됩니다. 파일 크기와 액세스 빈도는 다양합니다. 회사는 30일 이후에는 파일에 자주 액세스하지 않습니다. 그러나 사용자가 보고서를 생성하기 위해 이전 파일을 요청하는 경우가 있습니다.
이 회사는 자주 액세스하지 않는 파일의 저장 비용을 절감하고자 합니다. 또한 파일 시스템 크기에 따라 처리량을 조절하고자 합니다. TransitionToIA Amazon EFS 수명 주기 정책을 사용하여 30일 후 파일을 Infrequent Access(IA) 스토리지로 전환하고자 합니다.
어떤 솔루션이 이러한 요구 사항을 충족할까요?
> 정답 : C

- A. 버스팅 처리량 모드만 지정합니다.

- B. 프로비저닝된 처리량 모드만 지정합니다.

- C. 사용자가 파일에 다시 액세스할 때 파일이 표준 저장소로 다시 전환되도록 구성합니다. 버스팅 처리량 모드를 지정합니다.

- D. 사용자가 파일에 다시 액세스할 때 파일이 표준 저장소로 다시 전환되도록 구성합니다. 프로비저닝된 처리량 모드를 지정합니다.

---

# Q109. 한 회사가 온프레미스 관계형 데이터베이스를 보관해야 합니다. 회사는 데이터를 보관하고 싶어 합니다. 회사는 보관된 데이터에 대해 SQL 쿼리를 실행하여 연례 보고서를 작성할 수 있어야 합니다.
어떤 솔루션이 운영 비용을 최소화하면서 이러한 요구 사항을 충족할 수 있을까요?
> 정답 : A

- A. AWS Database Migration Service(AWS DMS)를 사용하여 온프레미스 데이터베이스를 Apache Parquet 형식으로 Amazon S3로 마이그레이션합니다. S3 Glacier Flexible Retrieval에 데이터를 저장합니다. Amazon Athena를 사용하여 보고서를 실행합니다.

- B. AWS Database Migration Service(AWS DMS)를 사용하여 온프레미스 데이터베이스를 Amazon RDS 인스턴스로 마이그레이션합니다. 온프레미스 데이터베이스를 폐기합니다. 보고서에 데이터가 필요할 때까지 RDS 인스턴스를 중지된 상태로 유지합니다.

- C. 온프레미스에서 데이터베이스 백업을 생성합니다. AWS DataSync를 사용하여 데이터를 Amazon S3로 전송합니다. S3 수명 주기 구성을 생성하여 데이터를 S3 Glacier Deep Archive로 이동합니다. 보고서를 실행하려면 백업을 Amazon EC2 인스턴스로 복원합니다.

- D. 온프레미스 데이터베이스에서 Amazon EC2 인스턴스로 데이터베이스 복제를 설정합니다. 온프레미스 데이터베이스를 폐기합니다. EC2 인스턴스의 스냅샷을 만듭니다. 보고서에 데이터가 필요할 때까지 EC2 인스턴스를 중지된 상태로 유지합니다.


---

# Q111. 여러 AWS 계정을 보유한 한 회사가 온프레미스 Microsoft Active Directory를 관리하고 있습니다. 이 회사는 직원들을 위한 싱글 사인온(SSO) 솔루션을 필요로 하며, AWS IAM Identity Center를 사용하려고 합니다.
해당 솔루션은 다음 요구 사항을 충족해야 합니다.
기존 Active Directory 자격 증명을 사용하여 사용자가 AWS 계정 및 타사 애플리케이션에 액세스할 수 있도록 합니다.
AWS 계정 접근 시 다단계 인증(MFA)을 적용하세요.
AWS 계정 및 애플리케이션에 대한 액세스 권한을 중앙에서 관리합니다.
> 정답 : D


- A. AWS 디렉터리 서비스를 사용하여 새 AWS 관리형 Microsoft AD Active Directory를 생성합니다. 각 계정의 IAM Identity Center에서 새 AWS 관리형 Microsoft AD Active Directory를 ID 소스로 사용하도록 구성합니다. IAM Identity Center를 사용하여 모든 사용자에 대해 MFA를 적용합니다.

- B. AWS Lambda 함수를 사용하여 Active Directory 사용자 및 그룹을 각 AWS 계정의 IAM 사용자 및 그룹과 주기적으로 동기화합니다. IAM 역할 및 정책을 사용하여 애플리케이션 액세스를 관리합니다.
다단계 인증(MFA)을 적용하기 위한 두 번째 Lambda 함수를 생성합니다.

- C. 각 AWS 계정에 Active Directory용 IAM ID 공급자를 생성합니다. Active Directory 사용자 및 그룹이 IAM 역할을 통해 AWS 계정에 직접 액세스할 수 있도록 합니다. IAM Identity Center를 사용하여 각 계정의 모든 사용자에 대해 다단계 인증(MFA)을 적용합니다.

- D. 기존 Active Directory를 ID 소스로 사용하여 IAM Identity Center를 활용합니다. 모든 사용자에게 MFA(다단계 인증)를 적용합니다. AWS Organizations와 Active Directory 그룹을 사용하여 AWS 계정 및 애플리케이션 액세스에 대한 액세스 권한을 관리합니다.

---

# Q112. 한 회사에서 온프레미스 데이터 센터에서 AWS 클라우드의 새로운 VPC로 새로운 애플리케이션을 마이그레이션하고 있습니다.
이 회사는 여러 서브넷과 애플리케이션을 공유하는 여러 AWS 계정과 VPC를 보유하고 있습니다. 이 회사는 새로운 애플리케이션에 대한 세분화된 액세스 제어를 원합니다. 이 회사는 새로운 애플리케이션에 액세스할 수 있는 권한이 부여된 계정과 VPC의 모든 네트워크 리소스가 애플리케이션에 액세스할 수 있도록 보장하고자 합니다.
어떤 솔루션이 이러한 요구 사항을 충족시킬까요?
> 정답 : B

- A. 새 애플리케이션 VPC에 액세스해야 하는 각 VPC에 대해 VPC 피어링 연결을 설정합니다. 각 VPC에서 경로 테이블을 업데이트하여 연결을 활성화합니다.

- B. AWS PrivateLink 엔드포인트 서비스를 사용하여 다른 VPC에서 새 애플리케이션에 액세스할 수 있도록 합니다. 엔드포인트 정책을 사용하여 애플리케이션에 대한 액세스를 제어합니다.

- C. 애플리케이션 로드 밸런서(ALB)를 사용하여 새 애플리케이션을 인터넷에 노출합니다. 인증 및 권한 부여 프로세스를 구성하여 지정된 VPC만 애플리케이션에 액세스할 수 있도록 합니다.

- D. 새 애플리케이션을 호스팅하는 계정에 전송 게이트웨이를 배포합니다. 애플리케이션에 연결해야 하는 각 계정과 전송 게이트웨이를 공유합니다. 새 애플리케이션을 호스팅하는 VPC와 전송 게이트웨이에서 경로 테이블을 업데이트하여 연결을 활성화합니다.


---

# Q116. 솔루션 아키텍트가 AWS 보안 계정의 타사 네트워크 방화벽을 통해 트래픽을 전송하는 데이터 보고 애플리케이션을 만들고 있습니다. 방화벽과 애플리케이션 서버는 부하 분산되어야 합니다.
이 애플리케이션은 TCP 연결을 사용하여 보고서를 생성합니다. 보고서는 몇 시간 동안 실행될 수 있으며, 최대 1시간 동안 유휴 상태가 될 수 있습니다. 유휴 기간 동안 보고서는 시간 초과되어서는 안 됩니다.
어떤 솔루션이 이러한 요구 사항을 충족시킬까요?
> 정답 : 

- A. 방화벽에는 게이트웨이 로드 밸런서(GWLB)를 사용하고, 애플리케이션 서버에는 애플리케이션 로드 밸런서(ALB)를 사용합니다. ALB, GWLB, 방화벽의 유휴 시간 초과 기간을 1시간으로 설정합니다.

- B. 보안 계정에 단일 방화벽을 사용합니다. 애플리케이션 서버에는 애플리케이션 부하 분산 장치(ALB)를 사용합니다. ALB 유휴 시간 초과와 방화벽 유휴 시간 초과를 1시간으로 설정합니다.

- C. 방화벽에는 게이트웨이 로드 밸런서(GWLB)를 사용하고, 애플리케이션 서버에는 애플리케이션 로드 밸런서(ALB)를 사용하십시오. ALB 유휴 시간 초과를 1시간으로 설정하십시오. 보고서 생성을 더 빨리 완료하려면 애플리케이션 서버 용량을 늘리십시오.

- D. 방화벽에는 게이트웨이 로드 밸런서(GWLB)를 사용하고, 애플리케이션 서버에는 애플리케이션 로드 밸런서(ALB)를 사용하세요. ALB 유휴 시간 초과 기간을 1시간으로 설정하세요.


---

# Q123. 한 회사에는 500마일(804.7km) 떨어져 있고 고속 광섬유 케이블로 연결된 주 데이터 센터와 보조 데이터 센터가 있습니다. 이 회사는 미션 크리티컬 워크로드를 위해 데이터 센터와 AWS VPC 간에 고가용성 및 보안 네트워크 연결이 필요합니다.
솔루션 아키텍트는 최대의 복원력을 제공하는 연결 솔루션을 선택해야 합니다.
어떤 솔루션이 이러한 요구 사항을 충족합니까?
> 정답 : C

- A. 동일한 장치의 한 Direct Connect 위치에서 종료되는 각 기본 및 보조 데이터 센터의 단일 AWS Direct Connect 연결

- B. 두 개의 별도 장치에서 하나의 Direct Connect 위치에서 종료되는 각 기본 및 보조 데이터 센터의 단일 AWS Direct Connect 연결

- C. 두 개의 별도 장치에 있는 두 개의 Direct Connect 위치에서 종료되는 기본 및 보조 데이터 센터 각각에서 두 개의 AWS Direct Connect 연결

- D. 두 개의 별도 장치에 있는 두 개의 Direct Connect 위치에서 종료되는 기본 데이터 센터의 두 개의 AWS Direct Connect 연결

---

# Q124. 한 회사가 Amazon S3에 기밀 데이터를 저장할 준비를 하고 있습니다. 규정 준수를 위해 저장 데이터는 암호화되어야 합니다. 암호화 키 사용은 감사 목적으로 기록되어야 합니다. 열쇠는 매년 교체해야 합니다.
이러한 요구 사항을 충족하고 운영상 가장 효율적인 솔루션은 무엇입니까?
> 정답 : A

- A. 자동 교체 기능이 있는 AWS KMS 키(SSE-KMS)를 사용한 서버 측 암호화

- B. 고객 제공 키를 사용한 서버 측 암호화(SSE-C)

- C. 수동 교체를 통한 AWS KMS 키(SSE-KMS)를 사용한 서버 측 암호화

- D. Amazon S3 관리형 키(SSE-S3)를 사용한 서버 측 암호화

---

# Q127. 한 회사가 쿠버네티스 클러스터에서 온프레미스 애플리케이션을 운영하고 있습니다. 최근 수백만 명의 신규 고객이 추가되었는데, 기존 온프레미스 인프라로는 대규모 신규 고객을 감당할 수 없습니다. 따라서 온프레미스 애플리케이션을 AWS 클라우드로 마이그레이션해야 합니다.
이 회사는 Amazon Elastic Kubernetes Service(Amazon EKS) 클러스터로 마이그레이션할 예정입니다. 회사는 AWS에서 새 아키텍처의 기본 컴퓨팅 인프라를 관리하고 싶지 않습니다.
어떤 솔루션이 운영 비용을 최소화하면서 이러한 요구 사항을 충족할 수 있을까요?
> 정답 : 

- A. AWS Fargate를 사용하여 컴퓨팅 용량을 제공합니다. Fargate 프로필을 생성하세요. Fargate 프로필을 사용하여 애플리케이션을 배포하세요.

- B. 자체 관리형 노드를 사용하여 컴퓨팅 용량을 공급합니다. 새 EKS 클러스터에 애플리케이션을 배포합니다.

- C. Karpenter와 함께 관리형 노드 그룹을 사용하여 컴퓨팅 용량을 공급합니다. 새 EKS 클러스터에 애플리케이션을 배포합니다.

- D. 관리형 노드 그룹을 사용하여 컴퓨팅 용량을 공급합니다. 새 EKS 클러스터에 애플리케이션을 배포합니다.


---

# Q128. 솔루션 아키텍트는 Auto Scaling 그룹의 Amazon EC2 인스턴스에서 실행되는 애플리케이션의 네트워크 아키텍처를 설계하고 있습니다. 애플리케이션은 Amazon S3 버킷에 있는 데이터에 액세스해야 합니다.
S3 버킷으로의 트래픽은 퍼블릭 IP 주소를 사용해서는 안 됩니다. 솔루션 아키텍트는 퍼블릭 및 프라이빗 서브넷이 있는 VPC에 애플리케이션을 배포합니다.
어떤 솔루션이 이러한 요구 사항을 충족시킬 수 있을까요? (2개를 선택하세요.)
> 정답 : B,D

- A. 프라이빗 서브넷에 EC2 인스턴스를 배포합니다. 고객 게이트웨이로의 기본 경로를 구성합니다.

- B. 퍼블릭 서브넷에 EC2 인스턴스를 배포합니다. Amazon S3에 대한 게이트웨이 엔드포인트를 만듭니다. 엔드포인트를 서브넷의 경로 테이블과 연결합니다.

- C. 프라이빗 서브넷에 EC2 인스턴스를 배포합니다. 이그레스 전용 인터넷 게이트웨이로의 기본 경로를 구성합니다.

- D. 프라이빗 서브넷에 EC2 인스턴스를 배포합니다. 퍼블릭 서브넷의 NAT 게이트웨이에 대한 기본 경로를 구성합니다.

- E. 퍼블릭 서브넷에 EC2 인스턴스를 배포합니다. Amazon S3에 대한 인터페이스 엔드포인트를 만듭니다. VPC에 대한 DNS 호스트 이름과 DNS 확인을 구성합니다.

---

# Q129. 한 회사가 자사 웹 애플리케이션에서 고객 활동을 포착하는 솔루션을 설계하고 있습니다. 회사는 활동 데이터를 분석하여 예측을 내리고자 합니다.
웹 애플리케이션에서의 고객 활동은 예측 불가능하며 갑자기 증가할 수 있습니다. 회사는 다른 웹 애플리케이션과 통합되는 솔루션이 필요합니다. 솔루션에는 권한 부여 단계가 포함되어야 합니다.
어떤 솔루션이 이러한 요구 사항을 충족시킬까요?
> 정답 : 


- A. Amazon Kinesis 데이터 스트림 앞에 Amazon API Gateway 엔드포인트를 배포합니다. Amazon S3 버킷에 데이터를 저장합니다. AWS Lambda 함수를 사용하여 권한 부여를 처리합니다.

- B. Amazon Elastic Container Service(Amazon ECS) 컨테이너 인스턴스 앞에 게이트웨이 로드 밸런서(GWLB)를 배포합니다. Amazon Elastic File System(Amazon EFS) 파일 시스템에 데이터를 저장합니다.
GWLB에 인증 헤더를 전달하도록 애플리케이션을 구성합니다.

- C. Amazon Data Firehose 전송 스트림 앞에 Amazon API Gateway 엔드포인트를 배포합니다. Amazon S3 버킷에 데이터를 저장합니다. API Gateway Lambda 권한 부여자를 사용하여 권한 부여를 처리합니다.

- D. Amazon Elastic Container Service(Amazon ECS) 컨테이너 인스턴스 앞에 게이트웨이 로드 밸런서(GWLB)를 배포합니다. Amazon Elastic File System(Amazon EFS) 파일 시스템에 데이터를 저장합니다.
AWS Lambda 함수를 사용하여 인증을 처리합니다.

---

# Q135. 한 회사가 여러 워크로드를 Amazon EC2 인스턴스로 마이그레이션할 계획이며, 적절한 AWS 계정 구조를 결정해야 합니다. 워크로드는 서로 격리되어야 하며, 각기 다른 사업부에 속해야 합니다. 또한, 회사는 통합된 월별 보기를 통해 각 사업부에 비용을 청구할 수 있어야 합니다.
어떤 솔루션이 관리 부담을 최소화하면서 이러한 요구 사항을 충족할까요?
> 정답 : 

- A. 각 사업부별로 별도의 독립형 AWS 계정을 생성합니다. 각 계정에서 AWS Cost Explorer API를 호출하여 비용 청구를 수행하는 스크립트를 생성합니다.

- B. 모든 사업부를 위한 단일 AWS 계정을 생성합니다. 사업부에 해당하는 EC2 인스턴스에 태그를 할당합니다. AWS Cost Explorer를 사용하여 비용 할당을 위한 태그를 활성화하여 비용 청구를 수행합니다.

- C. AWS Organizations에서 단일 조직을 생성합니다. 각 사업부별로 멤버 계정을 생성합니다.
조직 관리 계정의 청구서를 사용하여 비용 청구를 진행하세요.

- D. 각 사업부별로 AWS Organizations에 별도의 조직을 생성합니다. 각 조직 관리 계정의 청구서를 사용하여 비용 청구를 수행합니다.

---

# Q136. 한 회사에서 인터넷에 연결된 애플리케이션 부하 분산 장치(ALB)를 사용하여 웹 애플리케이션을 설계하고 있습니다.
이 회사는 ALB가 퍼블릭 인터넷에서 HTTPS 웹 트래픽을 수신해야 합니다. ALB는 포트 443에서 Amazon EC2 인스턴스에 호스팅된 웹 애플리케이션 서버에 HTTPS 트래픽만 보내야 합니다. ALB는 포트 8443에서 HTTPS를 통해 웹 애플리케이션 서버의 상태 검사를 수행해야 합니다.
ALB와 연관된 보안 그룹 구성의 어떤 조합이 이러한 요구 사항을 충족시킬까요? (3개를 선택하세요.)
> 정답 : A,C,F

- A. 포트 443에 대해 0.0.0.0/0에서 들어오는 HTTPS 트래픽을 허용합니다.

- B. 웹 애플리케이션 인스턴스에서 포트 443에 대한 HTTPS 인바운드 트래픽을 허용합니다.

- C. 포트 8443에서 상태 점검을 위해 웹 애플리케이션 인스턴스로의 HTTPS 아웃바운드 트래픽을 허용합니다.

- D. 포트 443에 대해 0.0.0.0/0으로의 모든 아웃바운드 트래픽을 허용합니다.

- E. 포트 8443에서 상태 점검을 위해 웹 애플리케이션 인스턴스에서 HTTPS 인바운드 트래픽을 허용합니다.

- F. 포트 443에 대해 웹 애플리케이션 인스턴스로의 HTTPS 아웃바운드 트래픽을 허용합니다.

---

# Q140. 한 회사가 Amazon ECS 클러스터에서 애플리케이션을 태스크로 실행합니다. 해당 애플리케이션은 특정 Amazon S3 버킷 그룹에 대한 읽기 및 쓰기 권한이 필요합니다. S3 버킷은 ECS 클러스터와 동일한 AWS 리전 및 AWS 계정에 있습니다. 회사는 최소 권한 원칙에 따라 애플리케이션에 S3 버킷에 대한 접근 권한을 부여해야 합니다.
다음 요구사항을 충족하는 해결책 조합은 무엇입니까? (두 가지를 선택하십시오.)
> 정답 : B,D

- A. ECS 작업의 인스턴스 역할에 IAM 정책을 연결합니다.

- B. 각 S3 버킷의 전체 Amazon 리소스 이름(ARN)을 나열하는 IAM 정책을 생성합니다.

- C. 모든 S3 버킷 이름 조합과 일치하는 와일드카드 Amazon 리소스 이름(ARN)을 포함하는 IAM 정책을 생성합니다.

- D. ECS 작업의 작업 역할에 IAM 정책을 연결합니다.

- E. 각 버킷에 태그를 추가합니다. 버킷의 태그와 값이 일치하는 문자열 조건이 포함된 IAM 정책을 생성합니다.